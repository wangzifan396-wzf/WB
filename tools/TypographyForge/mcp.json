{
  "id": "TypographyForge",
  "name": "typography_forge",
  "title": "TypographyForge",
  "description": "模块化字号比例（Modular Scale）：基准 × 比例^步数，内置常用比例预设，离线可用。",
  "entry": "modularScale",
  "entries": {
    "modularScale": {
      "paramStyle": "positional",
      "paramNames": [
        "base",
        "ratio",
        "steps"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "base": {
        "type": "string",
        "description": "第 1 个参数（base）"
      },
      "ratio": {
        "type": "string",
        "description": "第 2 个参数（ratio）"
      },
      "steps": {
        "type": "string",
        "description": "第 3 个参数（steps）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction modularScale(base, ratio, steps){\r\n  var out=[]; for(var i=0;i<=steps;i++){ out.push(+(base*Math.pow(ratio,i)).toFixed(4)); } return out;\r\n}\r\nvar PRESETS={minorSecond:1.067,majorSecond:1.125,minorThird:1.2,majorThird:1.25,perfectFourth:1.333,perfectFifth:1.5,golden:1.618};\r\nvar EXPORTS={modularScale:modularScale, PRESETS:PRESETS};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.TypographyForgePure = EXPORTS; }\r\n\r\n"
}