# ArchForge

架构图即代码。用 `web -> api` 一行一条边的极简 DSL 描述系统依赖，自动按最长路径分层布局，生成带箭头曲线的 SVG 架构图。

## 特性
- Kahn 拓扑分层 + 最长路径深度，循环依赖自动报错
- 每层独立着色、贝塞尔曲线连线、SVG 一键复制
- 纯前端、零依赖、离线可用

## 测试
```
node _test.js && node smoke.js
```
