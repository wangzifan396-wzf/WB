{
  "id": "MemeForge",
  "name": "meme_forge",
  "title": "MemeForge",
  "description": "表情包制作器：上传图片并叠加经典上下白边黑描边文字，自动换行缩放，导出表情包 PNG。",
  "entry": "wrapLines",
  "entries": {
    "wrapLines": {
      "paramStyle": "positional",
      "paramNames": [
        "text",
        "max"
      ],
      "fields": []
    },
    "fitFont": {
      "paramStyle": "positional",
      "paramNames": [
        "ctx",
        "text",
        "maxW",
        "start"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "第 1 个参数（text）"
      },
      "max": {
        "type": "string",
        "description": "第 2 个参数（max）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction wrapLines(text, max){\r\n  var words = String(text).split(\" \");\r\n  var lines = [], cur = \"\";\r\n  for (var i = 0; i < words.length; i++){\r\n    var cand = (cur ? cur + \" \" : \"\") + words[i];\r\n    if (cand.length > max && cur !== \"\"){ lines.push(cur); cur = words[i]; }\r\n    else cur = cand;\r\n  }\r\n  if (cur) lines.push(cur);\r\n  return lines;\r\n}\r\nfunction fitFont(ctx, text, maxW, start){\r\n  var f = start;\r\n  while (f > 8 && ctx.measureText(text).width > maxW){ f -= 2; ctx.font = \"bold \" + f + \"px Impact, sans-serif\"; }\r\n  return f;\r\n}\r\nvar EXPORTS = { wrapLines: wrapLines, fitFont: fitFont };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.MemeForgePure = EXPORTS; }\r\n\r\n"
}