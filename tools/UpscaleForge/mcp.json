{
  "id": "UpscaleForge",
  "name": "upscale_forge",
  "title": "UpscaleForge",
  "description": "最近邻/双线性放大图片并预览",
  "entry": "nearestMap",
  "entries": {
    "nearestMap": {
      "paramStyle": "positional",
      "paramNames": [
        "dw",
        "dh",
        "sw",
        "sh"
      ],
      "fields": []
    },
    "bilinearMap": {
      "paramStyle": "positional",
      "paramNames": [
        "dw",
        "dh",
        "sw",
        "sh"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "dw": {
        "type": "string",
        "description": "第 1 个参数（dw）"
      },
      "dh": {
        "type": "string",
        "description": "第 2 个参数（dh）"
      },
      "sw": {
        "type": "string",
        "description": "第 3 个参数（sw）"
      },
      "sh": {
        "type": "string",
        "description": "第 4 个参数（sh）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction nearestMap(dw,dh,sw,sh){ var out=[]; for(var y=0;y<dh;y++){ for(var x=0;x<dw;x++){ var sx=Math.min(sw-1, Math.round(x/dw*sw)); var sy=Math.min(sh-1, Math.round(y/dh*sh)); out.push(sy*sw+sx); } } return out; }\r\nfunction bilinearMap(dw,dh,sw,sh){ var out=[]; for(var y=0;y<dh;y++){ for(var x=0;x<dw;x++){ var fx=(x+0.5)/dw*sh-0.5, fy=(y+0.5)/dh*sw-0.5; out.push({sx:Math.max(0,Math.min(sh-1,fx)), sy:Math.max(0,Math.min(sw-1,fy))}); } } return out; }\r\nvar EXPORTS={nearestMap:nearestMap, bilinearMap:bilinearMap};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.UpscaleForgePure = EXPORTS; }\r\n\r\n"
}