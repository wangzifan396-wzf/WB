{
  "id": "ReverseTextForge",
  "name": "reverse_text_forge",
  "title": "文本反转",
  "description": "按码点（兼容 emoji）或按单词反转文本，离线。",
  "entry": "reverseCodepoints",
  "entries": {
    "reverseCodepoints": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "reverseWords": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction reverseCodepoints(s){ if(s==null) return ''; s=String(s); var out='', i=s.length-1;\r\n  while(i>=0){ var c=s.charCodeAt(i);\r\n    if(c>=0xDC00 && c<=0xDFFF && i>0){ var h=s.charCodeAt(i-1); if(h>=0xD800 && h<=0xDBFF){ out+=s.substr(i-1,2); i-=2; continue; } }\r\n    out+=s[i]; i--; }\r\n  return out; }\r\nfunction reverseWords(s){ if(s==null) return ''; return String(s).split(/\\s+/).reverse().join(' '); }\r\nvar EXPORTS={reverseCodepoints:reverseCodepoints, reverseWords:reverseWords};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ReverseTextForgePure = EXPORTS; }\r\n\r\n"
}