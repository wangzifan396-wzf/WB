{
  "id": "NotesForge",
  "name": "notes_forge",
  "title": "NotesForge",
  "description": "本地 Markdown 笔记，轻量 MD→HTML 预览，localStorage 保存。",
  "entry": "md",
  "entries": {
    "md": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "esc": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "inline": {
      "paramStyle": "single",
      "paramNames": [
        "t"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction esc(s){ return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }\r\nfunction md(s){\r\n  s=(s||''); var lines=s.split('\\n'), out=[], i=0;\r\n  while(i<lines.length){\r\n    var line=lines[i];\r\n    var h=/^(#{1,4})\\s+(.*)$/.exec(line);\r\n    if(h){ var lv=h[1].length; out.push('<h'+lv+'>'+esc(h[2])+'</h'+lv+'>'); i++; continue; }\r\n    if(/^[-*]\\s+/.test(line)){ var items=[]; while(i<lines.length && /^[-*]\\s+/.test(lines[i])){ items.push('<li>'+inline(lines[i].replace(/^[-*]\\s+/,''))+'</li>'); i++; } out.push('<ul>'+items.join('')+'</ul>'); continue; }\r\n    if(line.trim()===''){ i++; continue; }\r\n    out.push('<p>'+inline(line)+'</p>'); i++;\r\n  }\r\n  return out.join('\\n');\r\n}\r\nfunction inline(t){ t=esc(t); t=t.replace(/\\*\\*([^*]+)\\*\\*/g,'<b>$1</b>').replace(/\\*([^*]+)\\*/g,'<i>$1</i>').replace(/`([^`]+)`/g,'<code>$1</code>'); return t; }\r\nvar EXPORTS={md:md,esc:esc,inline:inline};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.NotesForgePure = EXPORTS; }\r\n\r\n"
}