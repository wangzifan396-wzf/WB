{
  "id": "LolcatForge",
  "name": "lolcat_forge",
  "title": "LolcatForge",
  "description": "彩虹文字：把每行文本按字符做 HSV 色相渐变，输出带内联样式的彩色文字，复制即用于文档或终端回放。",
  "entry": "rainbow",
  "entries": {
    "rainbow": {
      "paramStyle": "single",
      "paramNames": [
        "text"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "输入（text）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n(function(){\r\n  function hsv(h,s,v){ var c=v*s, x=c*(1-Math.abs((h/60)%2-1)), m=v-c, r,g,b;\r\n    if(h<60){r=c;g=x;}else if(h<120){r=x;g=c;}else if(h<180){g=c;b=x;}else if(h<240){g=x;b=c;}\r\n    else if(h<300){r=x;b=c;}else{r=c;b=x;}\r\n    return 'rgb('+Math.round((r+m)*255)+','+Math.round((g+m)*255)+','+Math.round((b+m)*255)+')'; }\r\n  function rainbow(text){\r\n    text=String(text==null?'':text); var out='', hue=0, step=12;\r\n    for(var i=0;i<text.length;i++){ var ch=text[i];\r\n      if(ch==='\\n'){ out+='\\n'; hue+=step; continue; }\r\n      if(ch===' '){ out+=' '; continue; }\r\n      out+='<span style=\"color:'+hsv(hue%360,0.85,1)+'\">'+ch.replace(/&/g,'&amp;').replace(/</g,'&lt;')+'</span>'; hue+=step; }\r\n    return {value:out};\r\n  }\r\n  var API={rainbow:rainbow};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__LolcatForge__=API;\r\n})();\r\n"
}