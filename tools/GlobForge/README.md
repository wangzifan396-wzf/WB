# GlobForge

离线 Glob 模式测试器 · 单文件 · 零依赖 · 本地优先。

## 功能
- 支持 `*`（不跨目录）、`**`（跨目录）、`?`、`[abc]` / `[!abc]` 字符类、`{a,b}` 分支
- 对一批路径实时匹配，输出命中列表与命中数
- 可选忽略大小写

## 纯函数
`globToRegExp` / `isMatch` / `filter` —— 见 `_test.js`。
