{
  "id": "UserAgentForge",
  "name": "user_agent_forge",
  "title": "UserAgentForge",
  "description": "User-Agent 解析：从 UA 字符串识别浏览器/版本/操作系统/设备类型，离线可用。",
  "entry": "parse",
  "entries": {
    "parse": {
      "paramStyle": "single",
      "paramNames": [
        "ua"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ua": {
        "type": "string",
        "description": "输入（ua）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction parse(ua){ ua=ua||'';\r\n  var browser='Unknown', version='';\r\n  var bmap=[['Edg','Edge'],['Chrome','Chrome'],['Firefox','Firefox'],['Safari','Safari'],['Opera','Opera'],['MSIE','IE'],['Trident','IE']];\r\n  for(var i=0;i<bmap.length;i++){ var idx=ua.indexOf(bmap[i][0]); if(idx>=0){ browser=bmap[i][1];\r\n    var v=ua.substring(idx+bmap[i][0].length+1); var vm=v.match(/^[\\d.]+/); if(vm) version=vm[0];\r\n    if(browser==='Safari'){ var sv=ua.match(/Version\\/([\\d.]+)/); if(sv) version=sv[1]; }\r\n    if(browser==='IE' && bmap[i][0]==='Trident'){ var rv=ua.match(/rv:([\\d.]+)/); if(rv) version=rv[1]; }\r\n    break; } }\r\n  var os='Unknown';\r\n  if(/(iPhone|iPod)/.test(ua)) os='iOS'; else if(/iPad/.test(ua)) os='iOS';\r\n  else if(/Windows NT 10/.test(ua)) os='Windows 10/11'; else if(/Windows/.test(ua)) os='Windows';\r\n  else if(/Mac OS X/.test(ua)) os='macOS'; else if(/Android/.test(ua)) os='Android';\r\n  else if(/Linux/.test(ua)) os='Linux';\r\n  var device='Desktop';\r\n  if(/(iPhone|iPod)/.test(ua)) device='Mobile'; else if(/iPad/.test(ua)) device='Tablet'; else if(/Mobile/.test(ua)) device='Mobile'; else if(/Tablet/.test(ua)) device='Tablet';\r\n  return {browser:browser, version:version, os:os, device:device}; }\r\nvar EXPORTS={parse:parse};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.UserAgentForgePure = EXPORTS; }\r\n\r\n"
}