{
  "id": "MetroForge",
  "name": "metro_forge",
  "title": "MetroForge",
  "description": "用 Web Audio 按 BPM 输出节拍声，标注速度与术语。",
  "entry": "intervalMs",
  "entries": {
    "intervalMs": {
      "paramStyle": "single",
      "paramNames": [
        "bpm"
      ],
      "fields": []
    },
    "subdivide": {
      "paramStyle": "positional",
      "paramNames": [
        "bpm",
        "div"
      ],
      "fields": []
    },
    "tempoName": {
      "paramStyle": "single",
      "paramNames": [
        "bpm"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "bpm": {
        "type": "string",
        "description": "输入（bpm）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction intervalMs(bpm){ return bpm>0 ? 60000/bpm : null; }\r\nfunction subdivide(bpm, div){ var base=intervalMs(bpm); return base? base/(div>0?div:1) : null; }\r\nfunction tempoName(bpm){ if(!(bpm>0))return '—'; if(bpm<60)return '慢板(Largo)'; if(bpm<76)return '行板(Andante)'; if(bpm<108)return '中板(Moderato)'; if(bpm<120)return '小快板(Allegretto)'; if(bpm<168)return '快板(Allegro)'; return '急板(Presto)'; }\r\nvar EXPORTS={intervalMs:intervalMs,subdivide:subdivide,tempoName:tempoName};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.MetroForgePure = EXPORTS; }\r\n\r\n"
}