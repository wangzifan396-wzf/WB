{
  "id": "CapacitanceForge",
  "name": "capacitance_forge",
  "title": "CapacitanceForge",
  "description": "电容计算：平行板电容 C=ε₀εᵣA/d，以及电容串、并联等效值计算。",
  "entry": "parallelPlate",
  "entries": {
    "parallelPlate": {
      "paramStyle": "positional",
      "paramNames": [
        "area",
        "dist",
        "er"
      ],
      "fields": []
    },
    "series": {
      "paramStyle": "single",
      "paramNames": [
        "arr"
      ],
      "fields": []
    },
    "parallel": {
      "paramStyle": "single",
      "paramNames": [
        "arr"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "area": {
        "type": "string",
        "description": "第 1 个参数（area）"
      },
      "dist": {
        "type": "string",
        "description": "第 2 个参数（dist）"
      },
      "er": {
        "type": "string",
        "description": "第 3 个参数（er）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar EPS0 = 8.854e-12;\r\nfunction parallelPlate(area, dist, er){ er=(er==null)?1:er; if(!(dist>0)) return NaN; return EPS0*er*area/dist; }\r\nfunction series(arr){ var s=0; for(var i=0;i<arr.length;i++){ if(!(arr[i]>0)) return NaN; s+=1/arr[i]; } return s===0?NaN:1/s; }\r\nfunction parallel(arr){ var s=0; for(var i=0;i<arr.length;i++){ s+=arr[i]; } return s; }\r\nvar EXPORTS={ EPS0:EPS0, parallelPlate:parallelPlate, series:series, parallel:parallel };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.CapacitanceForgePure = EXPORTS; }\r\n\r\n"
}