# MimeForge

离线 MIME 类型查询 · 单文件 · 零依赖 · 本地优先。

## 功能
- 扩展名（含带点 `json` / `.png`）→ MIME 类型
- MIME 类型 → 扩展名（可能多个）
- 内置 ~70 条常用映射，可扩展 `MIME_TABLE`

纯函数：`lookup` / `reverse` / `normExt` / `TABLE` —— 见 `_test.js`。
