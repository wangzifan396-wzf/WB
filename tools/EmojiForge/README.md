# EmojiForge

离线 emoji 速查与复制 · 单文件 · 零依赖 · 本地优先。

## 功能
- 内置 120+ 常用 emoji，按关键词（英文）或中文分组搜索
- 按分组（笑脸 / 手势 / 动物 / 自然 / 食物 / 物品 / 符号 / 旅行 / 活动）筛选
- 点击即复制，并显示 Unicode 码位（U+XXXX）

纯函数：`searchEmoji` / `byGroup` / `groups` / `codepoint` —— 见 `_test.js`。
