{
  "id": "AutocorrelationForge",
  "name": "autocorrelation_forge",
  "title": "AutocorrelationForge",
  "description": "自相关函数（ACF）：衡量序列与其滞后副本的相关性，识别周期性与时间依赖，离线精确。",
  "entry": "acf",
  "entries": {
    "acf": {
      "paramStyle": "positional",
      "paramNames": [
        "xs",
        "lag"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "xs": {
        "type": "string",
        "description": "第 1 个参数（xs）"
      },
      "lag": {
        "type": "string",
        "description": "第 2 个参数（lag）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction acf(xs, lag){ if(lag<0||lag>=xs.length) return NaN; var n=xs.length;\r\n  var mean=0; for(var i=0;i<n;i++) mean+=xs[i]; mean/=n;\r\n  var varc=0; for(var i=0;i<n;i++) varc+=(xs[i]-mean)*(xs[i]-mean);\r\n  if(varc===0) return lag===0?1:0;\r\n  var cov=0; for(var i=0;i<n-lag;i++) cov+=(xs[i]-mean)*(xs[i+lag]-mean);\r\n  return cov/varc;\r\n}\r\nvar EXPORTS={acf:acf};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.AutocorrelationForgePure = EXPORTS; }\r\n\r\n"
}