{
  "id": "AudioMergeForge",
  "name": "audio_merge_forge",
  "title": "音频合并",
  "description": "音频合并：将多个音频文件按顺序拼接为一段并导出 WAV，浏览器端解码，离线处理。",
  "entry": "concatSamples",
  "entries": {
    "concatSamples": {
      "paramStyle": "single",
      "paramNames": [
        "list"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "list": {
        "type": "string",
        "description": "输入（list）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction concatSamples(list){ var n=0,i; for(i=0;i<list.length;i++) n+=list[i].length; var out=new Float32Array(n); var o=0; for(i=0;i<list.length;i++){ out.set(list[i], o); o+=list[i].length; } return out; }\r\n\r\nvar EXPORTS={concatSamples:concatSamples};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.AudioMergeForgePure = EXPORTS; }\r\n\r\n"
}