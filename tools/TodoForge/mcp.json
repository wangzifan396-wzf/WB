{
  "id": "TodoForge",
  "name": "todo_forge",
  "title": "TodoForge",
  "description": "本地待办清单，localStorage 持久化，增删改与筛选。",
  "entry": "addItem",
  "entries": {
    "addItem": {
      "paramStyle": "positional",
      "paramNames": [
        "list",
        "text"
      ],
      "fields": []
    },
    "toggle": {
      "paramStyle": "positional",
      "paramNames": [
        "list",
        "id"
      ],
      "fields": []
    },
    "remove": {
      "paramStyle": "positional",
      "paramNames": [
        "list",
        "id"
      ],
      "fields": []
    },
    "filter": {
      "paramStyle": "positional",
      "paramNames": [
        "list",
        "mode"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "list": {
        "type": "string",
        "description": "第 1 个参数（list）"
      },
      "text": {
        "type": "string",
        "description": "第 2 个参数（text）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar SEQ=1;\r\nfunction addItem(list,text){ text=(text||'').trim(); if(!text) return list; return list.concat([{id:SEQ++,text:text,done:false}]); }\r\nfunction toggle(list,id){ return list.map(function(it){ return it.id===id?{id:it.id,text:it.text,done:!it.done}:it; }); }\r\nfunction remove(list,id){ return list.filter(function(it){ return it.id!==id; }); }\r\nfunction filter(list,mode){ if(mode==='active') return list.filter(function(it){return !it.done;}); if(mode==='done') return list.filter(function(it){return it.done;}); return list; }\r\nvar EXPORTS={addItem:addItem,toggle:toggle,remove:remove,filter:filter};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.TodoForgePure = EXPORTS; }\r\n\r\n"
}