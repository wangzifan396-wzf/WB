{
  "id": "HexToRgbForge",
  "name": "hex_to_rgb_forge",
  "title": "颜色解析",
  "description": "颜色解析：解析 HEX/RGB 为 RGB 与 HSL 并预览色块，支持 3/6/8 位 HEX，离线处理。",
  "entry": "parseColor",
  "entries": {
    "parseColor": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "rgbToHsl": {
      "paramStyle": "positional",
      "paramNames": [
        "r",
        "g",
        "b"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction parseColor(s){\r\n  s=(s||'').trim();\r\n  var m;\r\n  if((m=/^#([0-9a-fA-F]{3})$/.exec(s))){ var t=m[1]; return {ok:true, r:parseInt(t[0]+t[0],16), g:parseInt(t[1]+t[1],16), b:parseInt(t[2]+t[2],16), a:1}; }\r\n  if((m=/^#([0-9a-fA-F]{6})$/.exec(s))){ var h=m[1]; return {ok:true, r:parseInt(h.slice(0,2),16), g:parseInt(h.slice(2,4),16), b:parseInt(h.slice(4,6),16), a:1}; }\r\n  if((m=/^#([0-9a-fA-F]{8})$/.exec(s))){ var o=m[1]; return {ok:true, r:parseInt(o.slice(0,2),16), g:parseInt(o.slice(2,4),16), b:parseInt(o.slice(4,6),16), a:parseInt(o.slice(6,8),16)/255}; }\r\n  if(/^rgba?\\(/.test(s)){ var p=s.match(/[\\d.]+/g)||[]; if(p.length>=3) return {ok:true, r:+p[0], g:+p[1], b:+p[2], a:p.length>3?(+p[3]):1}; }\r\n  return {ok:false};\r\n}\r\nfunction rgbToHsl(r,g,b){ r/=255;g/=255;b/=255; var mx=Math.max(r,g,b),mn=Math.min(r,g,b),h,s,l=(mx+mn)/2; if(mx===mn){h=s=0;}else{var d=mx-mn; s=l>0.5?d/(2-mx-mn):d/(mx+mn); if(mx===r)h=(g-b)/d+(g<b?6:0); else if(mx===g)h=(b-r)/d+2; else h=(r-g)/d+4; h/=6;} return {h:Math.round(h*360),s:Math.round(s*100),l:Math.round(l*100)}; }\r\n\r\nvar EXPORTS={parseColor:parseColor,rgbToHsl:rgbToHsl};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.HexToRgbForgePure = EXPORTS; }\r\n\r\n"
}