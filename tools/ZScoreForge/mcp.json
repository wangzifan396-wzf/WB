{
  "id": "ZScoreForge",
  "name": "z_score_forge",
  "title": "ZScoreForge",
  "description": "标准分 Z-Score 与由列表求 Z，并用误差函数估算对应百分位，离线可用。",
  "entry": "zScore",
  "entries": {
    "zScore": {
      "paramStyle": "positional",
      "paramNames": [
        "x",
        "m",
        "sd"
      ],
      "fields": []
    },
    "zFromList": {
      "paramStyle": "positional",
      "paramNames": [
        "x",
        "list"
      ],
      "fields": []
    },
    "percentileFromZ": {
      "paramStyle": "single",
      "paramNames": [
        "z"
      ],
      "fields": []
    },
    "erf": {
      "paramStyle": "single",
      "paramNames": [
        "x"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "x": {
        "type": "string",
        "description": "第 1 个参数（x）"
      },
      "m": {
        "type": "string",
        "description": "第 2 个参数（m）"
      },
      "sd": {
        "type": "string",
        "description": "第 3 个参数（sd）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction zScore(x, m, sd){ if(!(sd > 0)) return NaN; return (x - m) / sd; }\r\nfunction zFromList(x, list){\r\n  var s=0; for(var i=0;i<list.length;i++) s+=list[i];\r\n  var m=s/list.length, v=0; for(var j=0;j<list.length;j++) v+=(list[j]-m)*(list[j]-m);\r\n  var sd=Math.sqrt(v/list.length);\r\n  return zScore(x, m, sd);\r\n}\r\nfunction percentileFromZ(z){ return 0.5 * (1 + erf(z/Math.SQRT2)); }\r\nfunction erf(x){\r\n  var t=1/(1+0.3275911*Math.abs(x)), y=1-(((((1.061405429*t-1.453152027)*t)+1.421413741)*t-0.284496736)*t+0.254829592)*t*Math.exp(-x*x);\r\n  return x>=0 ? y : -y;\r\n}\r\nvar EXPORTS = { zScore:zScore, zFromList:zFromList, percentileFromZ:percentileFromZ, erf:erf };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ZScoreForgePure = EXPORTS; }\r\n\r\n"
}