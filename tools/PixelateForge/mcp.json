{
  "id": "PixelateForge",
  "name": "pixelate_forge",
  "title": "PixelateForge",
  "description": "图片像素化（马赛克）：按块大小对图片分块取均值，本地 canvas 完成。",
  "entry": "pixelate",
  "entries": {
    "pixelate": {
      "paramStyle": "positional",
      "paramNames": [
        "src",
        "w",
        "h",
        "block"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "src": {
        "type": "string",
        "description": "第 1 个参数（src）"
      },
      "w": {
        "type": "string",
        "description": "第 2 个参数（w）"
      },
      "h": {
        "type": "string",
        "description": "第 3 个参数（h）"
      },
      "block": {
        "type": "string",
        "description": "第 4 个参数（block）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction pixelate(src, w, h, block){ if(!(block>=1)) block=1; block=Math.floor(block); var out=new Uint8ClampedArray(src.length); for(var by=0; by<h; by+=block){ for(var bx=0; bx<w; bx+=block){ var rs=0,gs=0,bs=0,as=0,cnt=0; var ex=Math.min(bx+block,w), ey=Math.min(by+block,h); for(var y=by;y<ey;y++){ for(var x=bx;x<ex;x++){ var idx=(y*w+x)*4; rs+=src[idx]; gs+=src[idx+1]; bs+=src[idx+2]; as+=src[idx+3]; cnt++; } } var R=Math.round(rs/cnt),G=Math.round(gs/cnt),B=Math.round(bs/cnt),A2=Math.round(as/cnt); for(var y=by;y<ey;y++){ for(var x=bx;x<ex;x++){ var idx=(y*w+x)*4; out[idx]=R; out[idx+1]=G; out[idx+2]=B; out[idx+3]=A2; } } } } return out; }\r\nvar EXPORTS={ pixelate:pixelate };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.PixelateForgePure = EXPORTS; }\r\n\r\n"
}