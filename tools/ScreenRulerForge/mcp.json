{
  "id": "ScreenRulerForge",
  "name": "screen_ruler_forge",
  "title": "ScreenRulerForge",
  "description": "屏幕像素↔毫米/英寸换算（按 DPI），并支持两点欧氏距离测量，辅助 UI 量取。",
  "entry": "pxToMm",
  "entries": {
    "pxToMm": {
      "paramStyle": "positional",
      "paramNames": [
        "px",
        "dpi"
      ],
      "fields": []
    },
    "pxToInch": {
      "paramStyle": "positional",
      "paramNames": [
        "px",
        "dpi"
      ],
      "fields": []
    },
    "mmToPx": {
      "paramStyle": "positional",
      "paramNames": [
        "mm",
        "dpi"
      ],
      "fields": []
    },
    "length": {
      "paramStyle": "positional",
      "paramNames": [
        "x1",
        "y1",
        "x2",
        "y2"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "px": {
        "type": "string",
        "description": "第 1 个参数（px）"
      },
      "dpi": {
        "type": "string",
        "description": "第 2 个参数（dpi）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction pxToMm(px, dpi){ if(!dpi) return NaN; return px/dpi*25.4; }\r\nfunction pxToInch(px, dpi){ if(!dpi) return NaN; return px/dpi; }\r\nfunction mmToPx(mm, dpi){ if(!dpi) return NaN; return mm/25.4*dpi; }\r\nfunction length(x1, y1, x2, y2){ return Math.hypot(x2-x1, y2-y1); }\r\nvar EXPORTS = { pxToMm:pxToMm, pxToInch:pxToInch, mmToPx:mmToPx, length:length };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ScreenRulerForgePure = EXPORTS; }\r\n\r\n"
}