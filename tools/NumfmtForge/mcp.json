{
  "id": "NumfmtForge",
  "name": "numfmt_forge",
  "title": "NumfmtForge",
  "description": "人类可读数字互转：1536 ↔ 1.5K，2,000,000 ↔ 2M。支持 K/M/G/T/P/E，方便阅读大数与还原精确值。",
  "entry": "toHuman",
  "entries": {
    "toHuman": {
      "paramStyle": "single",
      "paramNames": [
        "n"
      ],
      "fields": []
    },
    "fromHuman": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "n": {
        "type": "number",
        "description": "输入（n）（数值）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n(function(){\r\n  var UNITS=['','K','M','G','T','P','E'];\r\n  function toHuman(n){ n=Number(n); if(!isFinite(n)) return {error:'无效数字'}; if(n===0) return {value:'0'};\r\n    var neg=n<0; n=Math.abs(n); var u=0;\r\n    while(n>=1000 && u<UNITS.length-1){ n/=1000; u++; }\r\n    var s; if(u===0) s=String(Math.round(n));\r\n    else { s=(n>=100? n.toFixed(1): n.toFixed(2)).replace(/\\.?0+$/,''); s=s+' '+UNITS[u]; }\r\n    return {value:(neg?'-':'')+s}; }\r\n  function parseNum(s){ s=String(s).trim().toUpperCase();\r\n    var m=s.match(/^(-?\\d*\\.?\\d+)\\s*([KMGTPE])?$/); if(!m) return null;\r\n    var v=parseFloat(m[1]); if(m[2]) v*=Math.pow(10,{'K':3,'M':6,'G':9,'T':12,'P':15,'E':18}[m[2]]);\r\n    return Math.round(v); }\r\n  function fromHuman(s){ var v=parseNum(s); if(v===null) return {error:'无法解析: '+s}; return {value:String(v)}; }\r\n  var API={toHuman:toHuman,fromHuman:fromHuman};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__NumfmtForge__=API;\r\n})();\r\n"
}