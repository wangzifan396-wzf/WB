{
  "id": "BootstrapForge",
  "name": "bootstrap_forge",
  "title": "BootstrapForge",
  "description": "自助法 (Bootstrap)：有放回重采样估计统计量的置信区间（如均值），可设重采样次数与种子，离线。",
  "entry": "bootstrapCI",
  "entries": {
    "bootstrapCI": {
      "paramStyle": "positional",
      "paramNames": [
        "samples",
        "statFn",
        "B",
        "alpha",
        "seed"
      ],
      "fields": []
    },
    "mean": {
      "paramStyle": "single",
      "paramNames": [
        "a"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "samples": {
        "type": "string",
        "description": "第 1 个参数（samples）"
      },
      "statFn": {
        "type": "string",
        "description": "第 2 个参数（statFn）"
      },
      "B": {
        "type": "number",
        "description": "第 3 个参数（B）（数值）"
      },
      "alpha": {
        "type": "number",
        "description": "第 4 个参数（alpha）（数值）"
      },
      "seed": {
        "type": "number",
        "description": "第 5 个参数（seed）（数值）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction mulberry32(seed){ return function(){ seed|=0; seed=seed+0x6D2B79F5|0; var t=Math.imul(seed^seed>>>15,1|seed); t=t+Math.imul(t^t>>>7,61|t)^t; return ((t^t>>>14)>>>0)/4294967296; }; }\r\nfunction mean(a){ var s=0; for(var i=0;i<a.length;i++) s+=a[i]; return s/a.length; }\r\nfunction bootstrapCI(samples, statFn, B, alpha, seed){\r\n  B=B||2000; alpha=alpha||0.05; var rnd=mulberry32(seed||1);\r\n  var n=samples.length, stats=[];\r\n  for(var b=0;b<B;b++){ var rs=[]; for(var i=0;i<n;i++){ var idx=Math.floor(rnd()*n); rs.push(samples[idx]); } stats.push(statFn(rs)); }\r\n  stats.sort(function(a,b){return a-b;});\r\n  var lo=stats[Math.floor((alpha/2)*B)], hi=stats[Math.floor((1-alpha/2)*B)];\r\n  return {lo:lo, hi:hi, B:B, mean:mean(samples)};\r\n}\r\nvar EXPORTS={bootstrapCI:bootstrapCI, mean:mean};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.BootstrapForgePure = EXPORTS; }\r\n\r\n"
}