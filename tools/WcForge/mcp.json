{
  "id": "WcForge",
  "name": "wc_forge",
  "title": "WcForge",
  "description": "文本统计：实时统计行数、词数、字符数与 UTF-8 字节数，等价于 wc -lwm，粘贴即算。",
  "entry": "count",
  "entries": {
    "count": {
      "paramStyle": "single",
      "paramNames": [
        "text"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "输入（text）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n(function(){\r\n  function count(text){\r\n    text=String(text==null?'':text);\r\n    var lines = text==='' ? 0 : text.split('\\n').filter(function(l,i,a){ return !(i===a.length-1 && l===''); }).length;\r\n    var words=(text.trim().match(/\\S+/g)||[]).length;\r\n    var chars=text.length;\r\n    var bytes=0; for(var i=0;i<text.length;i++){ var c=text.charCodeAt(i);\r\n      if(c<0x80)bytes++; else if(c<0x800)bytes+=2; else if(c>=0xD800&&c<=0xDBFF)bytes+=4,i++; else bytes+=3; }\r\n    return {value:{lines:lines,words:words,chars:chars,bytes:bytes}};\r\n  }\r\n  var API={count:count};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__WcForge__=API;\r\n})();\r\n"
}