{
  "id": "BinomialForge",
  "name": "binomial_forge",
  "title": "BinomialForge",
  "description": "二项分布 Binomial(n,p)：单点概率 P(X=k) 与累积概率 P(X≤k)，组合数 BigInt 精确。",
  "entry": "nCr",
  "entries": {
    "nCr": {
      "paramStyle": "positional",
      "paramNames": [
        "n",
        "k"
      ],
      "fields": []
    },
    "pmf": {
      "paramStyle": "positional",
      "paramNames": [
        "n",
        "k",
        "p"
      ],
      "fields": []
    },
    "cdf": {
      "paramStyle": "positional",
      "paramNames": [
        "n",
        "k",
        "p"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "n": {
        "type": "string",
        "description": "第 1 个参数（n）"
      },
      "k": {
        "type": "string",
        "description": "第 2 个参数（k）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction nCr(n, k){ n=BigInt(n); k=BigInt(k); if(k<0n||k>n) return 0n; k=k<n-k?k:(n-k); var r=1n; for(var i=0n;i<k;i++){ r=r*(n-i)/(i+1n); } return r; }\r\nfunction pmf(n, k, p){ if(k<0||k>n) return 0; return Number(nCr(n,k))*Math.pow(p,k)*Math.pow(1-p,n-k); } // P(X=k)\r\nfunction cdf(n, k, p){ if(k<0) return 0; if(k>=n) return 1; var s=0; for(var i=0;i<=k;i++) s+=Number(nCr(n,i))*Math.pow(p,i)*Math.pow(1-p,n-i); return s; } // P(X<=k)\r\nvar EXPORTS = { nCr:nCr, pmf:pmf, cdf:cdf };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.BinomialForgePure = EXPORTS; }\r\n\r\n"
}