{
  "id": "BaconForge",
  "name": "bacon_forge",
  "title": "BaconForge",
  "description": "培根密码（Baconian cipher）：把每个字母编码成 5 位 a/b（A=aaaaa …），是最早的二进制编码思想之一，也常被用于字体隐写。支持编码与解码，纯本地、零依赖。",
  "entry": "bcEnc",
  "entries": {
    "bcEnc": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "bcDec": {
      "paramStyle": "single",
      "paramNames": [
        "code"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar BC_A=\"ABCDEFGHIKLMNOPQRSTUVWXYZ\";\r\nfunction bcBuild(){ var m={}; for(var i=0;i<BC_A.length;i++){ var b=(i).toString(2); while(b.length<5) b='0'+b; m[BC_A[i]]=b.replace(/0/g,'a').replace(/1/g,'b'); } return m; }\r\nvar BC_MAP=bcBuild();\r\nfunction bcEnc(s){ s=s.toUpperCase().replace(/[^A-Z]/g,'').replace(/J/g,'I').replace(/U/g,'V'); var out=\"\"; for(var i=0;i<s.length;i++) out+=BC_MAP[s[i]]; var r=\"\"; for(var i=0;i<out.length;i++) r+= String.fromCharCode(65+out.charCodeAt(i)-97); return r; }\r\nfunction bcDec(code){ code=code.toUpperCase().replace(/[^A-Z]/g,''); var out=\"\"; for(var i=0;i+5<=code.length;i+=5){ var bits=code.slice(i,i+5).toLowerCase().replace(/a/g,'0').replace(/b/g,'1'); var v=parseInt(bits,2); if(v>=0&&v<BC_A.length) out+=BC_A[v]; } return out; }\r\nvar EXPORTS={bcEnc:bcEnc, bcDec:bcDec};\r\n\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.BaconForgePure = EXPORTS; }\r\n\r\n"
}