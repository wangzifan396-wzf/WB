{
  "id": "FaviconForge",
  "name": "favicon_forge",
  "title": "FaviconForge",
  "description": "离线 Favicon 生成：文字 / Emoji → SVG，自定义配色。",
  "entry": "faviconSvg",
  "entries": {
    "faviconSvg": {
      "paramStyle": "positional",
      "paramNames": [
        "input",
        "opts"
      ],
      "fields": []
    },
    "initials": {
      "paramStyle": "single",
      "paramNames": [
        "text"
      ],
      "fields": []
    },
    "colorFromText": {
      "paramStyle": "single",
      "paramNames": [
        "t"
      ],
      "fields": []
    },
    "escapeXml": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "input": {
        "type": "string",
        "description": "第 1 个参数（input）"
      },
      "opts": {
        "type": "object",
        "description": "第 2 个参数（opts）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\nfunction hashStr(s){ s=String(s); var h=2166136261; for(var i=0;i<s.length;i++){ h^=s.charCodeAt(i); h=(h*16777619)>>>0; } return h>>>0; }\r\nfunction colorFromText(t){ return 'hsl('+(hashStr(t)%360)+',62%,52%)'; }\r\nfunction initials(text){\r\n  text=String(text).trim(); if(!text) return '?';\r\n  var parts=text.split(/\\s+/);\r\n  if(parts.length>=2) return (parts[0][0]+parts[1][0]).toUpperCase();\r\n  return text[0].toUpperCase();\r\n}\r\nfunction escapeXml(s){ return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;'); }\r\nfunction faviconSvg(input, opts){\r\n  opts=opts||{};\r\n  var bg=opts.bg||colorFromText(input);\r\n  var fg=opts.fg||'#FFFFFF';\r\n  var size=64;\r\n  var content;\r\n  if(opts.emoji){ content='<text x=\"32\" y=\"44\" font-size=\"38\" text-anchor=\"middle\">'+escapeXml(input)+'</text>'; }\r\n  else { content='<text x=\"32\" y=\"42\" font-size=\"30\" font-weight=\"700\" text-anchor=\"middle\" fill=\"'+fg+'\">'+escapeXml(initials(input))+'</text>'; }\r\n  return '<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"'+size+'\" height=\"'+size+'\" viewBox=\"0 0 '+size+' '+size+'\"><rect width=\"'+size+'\" height=\"'+size+'\" rx=\"14\" fill=\"'+bg+'\"/></rect>'+content+'</svg>';\r\n}\r\nif (typeof module !== 'undefined' && module.exports) {\r\n  module.exports = { faviconSvg:faviconSvg, initials:initials, colorFromText:colorFromText, escapeXml:escapeXml };\r\n}\r\nif (typeof window !== 'undefined') {\r\n  window.FaviconForgePure = { faviconSvg:faviconSvg, initials:initials, colorFromText:colorFromText, escapeXml:escapeXml };\r\n}\r\n"
}