{
  "id": "KalmanForge",
  "name": "kalman_forge",
  "title": "KalmanForge",
  "description": "卡尔曼滤波（一维常量模型）：由带噪观测递推估计真值，预测—更新两步，离线精确。",
  "entry": "kalman",
  "entries": {
    "kalman": {
      "paramStyle": "positional",
      "paramNames": [
        "zs",
        "q",
        "r",
        "x0",
        "p0"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "zs": {
        "type": "string",
        "description": "第 1 个参数（zs）"
      },
      "q": {
        "type": "string",
        "description": "第 2 个参数（q）"
      },
      "r": {
        "type": "string",
        "description": "第 3 个参数（r）"
      },
      "x0": {
        "type": "string",
        "description": "第 4 个参数（x0）"
      },
      "p0": {
        "type": "string",
        "description": "第 5 个参数（p0）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction kalman(zs, q, r, x0, p0){\r\n  var x=(x0===undefined?0:x0), p=(p0===undefined?1:p0); var out=[];\r\n  for(var i=0;i<zs.length;i++){\r\n    p = p + q;\r\n    var k = p/(p+r);\r\n    x = x + k*(zs[i]-x);\r\n    p = (1-k)*p;\r\n    out.push(x);\r\n  }\r\n  return out;\r\n}\r\nvar EXPORTS={kalman:kalman};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.KalmanForgePure = EXPORTS; }\r\n\r\n"
}