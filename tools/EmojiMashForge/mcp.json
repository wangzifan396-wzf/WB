{
  "id": "EmojiMashForge",
  "name": "emoji_mash_forge",
  "title": "EmojiMashForge",
  "description": "Emoji 组合器：把两个 Emoji 以并排 / 上下 / 重叠方式组合到画布，导出 PNG——做自己的 Emoji Kitchen。",
  "entry": "layout",
  "entries": {
    "layout": {
      "paramStyle": "positional",
      "paramNames": [
        "mode",
        "w",
        "h"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "description": "第 1 个参数（mode）"
      },
      "w": {
        "type": "string",
        "description": "第 2 个参数（w）"
      },
      "h": {
        "type": "string",
        "description": "第 3 个参数（h）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction layout(mode, w, h){\r\n  if (mode === \"side\") return [{ x: w * 0.27, y: h/2, s: h * 0.5 }, { x: w * 0.73, y: h/2, s: h * 0.5 }];\r\n  if (mode === \"top\") return [{ x: w/2, y: h * 0.28, s: h * 0.45 }, { x: w/2, y: h * 0.74, s: h * 0.45 }];\r\n  return [{ x: w * 0.4, y: h/2, s: h * 0.6 }, { x: w * 0.6, y: h/2, s: h * 0.6 }];\r\n}\r\nvar EXPORTS = { layout: layout };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.EmojiMashForgePure = EXPORTS; }\r\n\r\n"
}