{
  "id": "AudioReverseForge",
  "name": "audio_reverse_forge",
  "title": "AudioReverseForge",
  "description": "将音频采样反向（单声道或交错立体声）。",
  "entry": "reverse",
  "entries": {
    "reverse": {
      "paramStyle": "single",
      "paramNames": [
        "samples"
      ],
      "fields": []
    },
    "reverseInterleaved": {
      "paramStyle": "positional",
      "paramNames": [
        "samples",
        "ch"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "samples": {
        "type": "string",
        "description": "输入（samples）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction reverse(samples){ if(!samples||!samples.length) return []; var n=samples.length, out=new Array(n); for(var i=0;i<n;i++) out[i]=samples[n-1-i]; return out; }\r\nfunction reverseInterleaved(samples, ch){\r\n  if(!(ch>0)) return [];\r\n  var n=Math.floor(samples.length/ch), out=new Array(samples.length);\r\n  for(var i=0;i<n;i++){ for(var c=0;c<ch;c++) out[i*ch+c]=samples[(n-1-i)*ch+c]; }\r\n  return out;\r\n}\r\nvar EXPORTS={reverse:reverse,reverseInterleaved:reverseInterleaved};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.AudioReverseForgePure = EXPORTS; }\r\n\r\n"
}