{
  "id": "LotteryForge",
  "name": "lottery_forge",
  "title": "LotteryForge",
  "description": "彩票机选工具：在号码池中随机抽取不重复号码，支持多注、可排除特定号码，结果按从小到大排序，方便直接对照。",
  "entry": "pick",
  "entries": {
    "pick": {
      "paramStyle": "positional",
      "paramNames": [
        "pool",
        "count",
        "tickets",
        "exclude",
        "rng"
      ],
      "fields": []
    },
    "rngFrom": {
      "paramStyle": "single",
      "paramNames": [
        "seed"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "pool": {
        "type": "number",
        "description": "第 1 个参数（pool）（数值）"
      },
      "count": {
        "type": "number",
        "description": "第 2 个参数（count）（数值）"
      },
      "tickets": {
        "type": "number",
        "description": "第 3 个参数（tickets）（数值）"
      },
      "exclude": {
        "type": "array",
        "description": "第 4 个参数（exclude）"
      },
      "rng": {
        "type": "string",
        "description": "第 5 个参数（rng）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\n(function(){\r\n  function rngFrom(seed){var s=(seed>>>0)||1;return function(){s=(s*1664525+1013904223)>>>0;return s/4294967296;};}\r\n  function pick(pool,count,tickets,exclude,rng){pool=pool||49;count=count||6;tickets=tickets||1;rng=rng||Math.random;exclude=exclude||[];var ex={};exclude.forEach(function(x){ex[x]=1;});var out=[];\r\n    for(var t=0;t<tickets;t++){var avail=[];for(var i=1;i<=pool;i++)if(!ex[i])avail.push(i);var chosen=[];for(var k=0;k<count&&avail.length;k++){var idx=Math.floor(rng()*avail.length);chosen.push(avail.splice(idx,1)[0]);}chosen.sort(function(a,b){return a-b;});out.push(chosen);}\r\n    return {value:{tickets:out,pool:pool,count:count}};\r\n  }\r\n  var API={pick:pick,rngFrom:rngFrom};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__LotteryForge__=API;\r\n})();\r\n\r\n"
}