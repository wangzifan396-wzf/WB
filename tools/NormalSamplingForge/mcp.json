{
  "id": "NormalSamplingForge",
  "name": "normal_sampling_forge",
  "title": "NormalSamplingForge",
  "description": "Box-Muller 变换由均匀分布生成正态分布随机数（可设种子，结果可复现），离线。",
  "entry": "mulberry32",
  "entries": {
    "mulberry32": {
      "paramStyle": "single",
      "paramNames": [
        "seed"
      ],
      "fields": []
    },
    "sampleN": {
      "paramStyle": "positional",
      "paramNames": [
        "mu",
        "sigma",
        "n",
        "seed"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "seed": {
        "type": "string",
        "description": "输入（seed）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction mulberry32(seed){ return function(){ seed|=0; seed=seed+0x6D2B79F5|0; var t=Math.imul(seed^seed>>>15,1|seed); t=t+Math.imul(t^t>>>7,61|t)^t; return ((t^t>>>14)>>>0)/4294967296; }; }\r\nfunction sampleN(mu, sigma, n, seed){ var rnd=mulberry32(seed||1); var out=[]; for(var i=0;i<n;i++){ var u1=rnd(),u2=rnd(); if(u1<1e-12)u1=1e-12; var z=Math.sqrt(-2*Math.log(u1))*Math.cos(2*Math.PI*u2); out.push(mu+sigma*z); } return out; }\r\nvar EXPORTS={mulberry32:mulberry32, sampleN:sampleN};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.NormalSamplingForgePure = EXPORTS; }\r\n\r\n"
}