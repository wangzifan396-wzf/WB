{
  "id": "PolybiusForge",
  "name": "polybius_forge",
  "title": "PolybiusForge",
  "description": "波利比奥斯方阵（Polybius Square）密码：把每个字母映射到 5×5 方阵里的行列坐标（如 A=11、B=12），是古希腊的古典替代密码，也常用于间谍通信。支持编码与解码，纯本地、零依赖。",
  "entry": "pbEnc",
  "entries": {
    "pbEnc": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "pbDec": {
      "paramStyle": "single",
      "paramNames": [
        "code"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar PB_ALPHA=\"ABCDEFGHIKLMNOPQRSTUVWXYZ\";\r\nfunction pbFind(ch){ ch=ch.toUpperCase(); if(ch==='J')ch='I'; var i=PB_ALPHA.indexOf(ch); if(i<0) return null; return [Math.floor(i/5)+1, (i%5)+1]; }\r\nfunction pbEnc(s){ var out=\"\"; for(var i=0;i<s.length;i++){ var p=pbFind(s[i]); if(p) out+=p[0]+''+p[1]+' '; } return out.trim(); }\r\nfunction pbDec(code){ code=code.replace(/[^0-9]/g,''); var out=\"\"; for(var i=0;i+1<code.length;i+=2){ var r=parseInt(code[i],10), c=parseInt(code[i+1],10); if(r>=1&&r<=5&&c>=1&&c<=5){ var idx=(r-1)*5+(c-1); out+=PB_ALPHA[idx]; } } return out; }\r\nvar EXPORTS={pbEnc:pbEnc, pbDec:pbDec};\r\n\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.PolybiusForgePure = EXPORTS; }\r\n\r\n"
}