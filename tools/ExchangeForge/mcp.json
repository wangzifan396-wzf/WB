{
  "id": "ExchangeForge",
  "name": "exchange_forge",
  "title": "ExchangeForge",
  "description": "按手动汇率在两种货币间换算，支持手续费与逆向换算。",
  "entry": "convert",
  "entries": {
    "convert": {
      "paramStyle": "positional",
      "paramNames": [
        "amount",
        "rate"
      ],
      "fields": []
    },
    "inverse": {
      "paramStyle": "positional",
      "paramNames": [
        "amount",
        "rate"
      ],
      "fields": []
    },
    "convertWithFee": {
      "paramStyle": "positional",
      "paramNames": [
        "amount",
        "rate",
        "feePct"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "amount": {
        "type": "string",
        "description": "第 1 个参数（amount）"
      },
      "rate": {
        "type": "string",
        "description": "第 2 个参数（rate）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction convert(amount, rate){ if(!(amount>=0)||!(rate>0)) return null; return amount*rate; }\r\nfunction inverse(amount, rate){ if(!(amount>=0)||!(rate>0)) return null; return amount/rate; }\r\nfunction convertWithFee(amount, rate, feePct){ if(!(amount>=0)||!(rate>0)||!(feePct>=0&&feePct<100)) return null; return amount*rate*(1-feePct/100); }\r\nvar EXPORTS={convert:convert,inverse:inverse,convertWithFee:convertWithFee};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ExchangeForgePure = EXPORTS; }\r\n\r\n"
}