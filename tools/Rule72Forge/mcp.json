{
  "id": "Rule72Forge",
  "name": "rule72_forge",
  "title": "72 法则",
  "description": "用 72/69.3 法则与精确值估算资产翻倍年限，离线。",
  "entry": "rule72",
  "entries": {
    "rule72": {
      "paramStyle": "single",
      "paramNames": [
        "rate"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "rate": {
        "type": "string",
        "description": "输入（rate）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction rule72(rate){ rate=+rate; if(!(rate>0)) return {error:'利率须为正'};\r\n  var r=rate/100;\r\n  return {rule72:Math.round(72/rate*100)/100, rule69:Math.round(69.3/rate*100)/100,\r\n          exact:Math.round(Math.log(2)/Math.log(1+r)*100)/100, rate:rate}; }\r\nvar EXPORTS={rule72:rule72};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.Rule72ForgePure = EXPORTS; }\r\n\r\n"
}