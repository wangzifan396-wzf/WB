{
  "id": "AdlerForge",
  "name": "adler_forge",
  "title": "AdlerForge",
  "description": "Adler-32 校验和：计算文本或字节流的 32 位校验值，常用于 zlib 校验与文件完整性快速比对，离线计算。",
  "entry": "sum",
  "entries": {
    "sum": {
      "paramStyle": "single",
      "paramNames": [
        "str"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "str": {
        "type": "string",
        "description": "输入（str）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n(function(){\r\n  function sum(str){ str=String(str==null?'':str); var a=1,b=0, MOD=65521;\r\n    for(var i=0;i<str.length;i++){ var c=str.charCodeAt(i); if(c>255) c&=0xff; a=(a+c)%MOD; b=(b+a)%MOD; }\r\n    var v=((b<<16)|a)>>>0; return {value: ('00000000'+v.toString(16)).slice(-8).toLowerCase()}; }\r\n  var API={sum:sum};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__AdlerForge__=API;\r\n})();\r\n"
}