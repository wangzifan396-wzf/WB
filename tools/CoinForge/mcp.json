{
  "id": "CoinForge",
  "name": "coin_forge",
  "title": "CoinForge",
  "description": "抛硬币工具：一次抛掷多枚，可设置正面概率（模拟加权硬币），显示正面/反面次数与正反面序列，结果可复制。",
  "entry": "flip",
  "entries": {
    "flip": {
      "paramStyle": "positional",
      "paramNames": [
        "n",
        "bias",
        "rng"
      ],
      "fields": []
    },
    "rngFrom": {
      "paramStyle": "single",
      "paramNames": [
        "seed"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "n": {
        "type": "number",
        "description": "第 1 个参数（n）（数值）"
      },
      "bias": {
        "type": "string",
        "description": "第 2 个参数（bias）"
      },
      "rng": {
        "type": "string",
        "description": "第 3 个参数（rng）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\n(function(){\r\n  function rngFrom(seed){var s=(seed>>>0)||1;return function(){s=(s*1664525+1013904223)>>>0;return s/4294967296;};}\r\n  function flip(n,bias,rng){n=n||1;bias=(bias==null?0.5:bias);rng=rng||Math.random;var seq=[],h=0,t=0,i;for(i=0;i<n;i++){if(rng()<bias){h++;seq.push('H');}else{t++;seq.push('T');}}return {value:{heads:h,tails:t,sequence:seq}};}\r\n  var API={flip:flip,rngFrom:rngFrom};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__CoinForge__=API;\r\n})();\r\n\r\n"
}