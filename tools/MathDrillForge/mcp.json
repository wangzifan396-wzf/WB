{
  "id": "MathDrillForge",
  "name": "math_drill_forge",
  "title": "口算练习",
  "description": "口算练习：按种子生成加减乘除口算题，可调题数/上限/运算，可隐藏答案，离线处理。",
  "entry": "mathDrill",
  "entries": {
    "mathDrill": {
      "paramStyle": "object",
      "paramNames": [
        "opts"
      ],
      "fields": [
        "n",
        "ops",
        "seed",
        "hideAnswers"
      ]
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "n": {
        "type": "string",
        "description": "参数 n"
      },
      "ops": {
        "type": "string",
        "description": "参数 ops"
      },
      "seed": {
        "type": "string",
        "description": "参数 seed"
      },
      "hideAnswers": {
        "type": "string",
        "description": "参数 hideAnswers"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nfunction mathDrill(opts){\r\n  opts=opts||{}; var n=opts.n!=null?opts.n:20; if(n<1)n=1; else if(n>100)n=100;\r\n  var ops=String(opts.ops||'+-'); var max=opts.max!=null?opts.max:20;\r\n  var seed=opts.seed!=null?opts.seed:1; var r=rngFactory(seed);\r\n  var out=[];\r\n  for(var i=0;i<n;i++){\r\n    var op=ops.charAt(Math.floor(r()*ops.length));\r\n    var a,b,c,expr,ans;\r\n    if(op==='+'){ a=Math.floor(r()*max)+1; b=Math.floor(r()*max)+1; expr=a+' + '+b; ans=a+b; }\r\n    else if(op==='-'){ a=Math.floor(r()*max)+2; b=Math.floor(r()*a)+1; expr=a+' - '+b; ans=a-b; }\r\n    else if(op==='*'){ a=Math.floor(r()*12)+2; b=Math.floor(r()*12)+2; expr=a+' × '+b; ans=a*b; }\r\n    else { b=Math.floor(r()*11)+2; c=Math.floor(r()*11)+2; a=b*c; expr=a+' ÷ '+b; ans=c; }\r\n    out.push(opts.hideAnswers ? (expr+' =') : (expr+' = '+ans));\r\n  }\r\n  return out.join('\\n');\r\n}\r\n\r\nvar EXPORTS={mathDrill:mathDrill};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.MathDrillForgePure = EXPORTS; }\r\n\r\n"
}