# QsForge

URL 查询串解析 / 序列化 —— 单文件、零依赖、本地优先。解析 `a=1&a=2` 为数组、解码 `%20` 与 `+`、处理前导 `?`，并反向序列化（2026 年 Web 全栈 / API 网关 / 边缘函数对轻量查询处理的持续需求）。

- `qsParse(qs)` → 对象（重复键合并为数组，单键为字符串）。
- `qsStringify(obj)` → 查询串（数组展开为重复键）。
- 纯函数，结果可断言。

## 测试
```
node _test.js
node smoke.js
```
