{
  "id": "QuartileForge",
  "name": "quartile_forge",
  "title": "QuartileForge",
  "description": "四分位数 Q1/Q2/Q3 与四分位距 IQR（线性插值法，同 NumPy 默认），附最小/最大。",
  "entry": "quartiles",
  "entries": {
    "quartiles": {
      "paramStyle": "single",
      "paramNames": [
        "arr"
      ],
      "fields": []
    },
    "quantileSorted": {
      "paramStyle": "positional",
      "paramNames": [
        "arr",
        "q"
      ],
      "fields": []
    },
    "mean": {
      "paramStyle": "single",
      "paramNames": [
        "a"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "arr": {
        "type": "string",
        "description": "输入（arr）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction mean(a){ return a.reduce(function(s,x){return s+x;},0)/a.length; }\r\nfunction quantileSorted(arr, q){ var n=arr.length; if(n===0) return NaN; var pos=(n-1)*q; var lo=Math.floor(pos), hi=Math.ceil(pos); if(lo===hi) return arr[lo]; return arr[lo]+(arr[hi]-arr[lo])*(pos-lo); }\r\nfunction quartiles(arr){ if(!arr.length) return null; var a=arr.slice().sort(function(x,y){return x-y;}); var q1=quantileSorted(a,0.25), q2=quantileSorted(a,0.5), q3=quantileSorted(a,0.75); return {q1:q1, q2:q2, q3:q3, iqr:q3-q1, min:a[0], max:a[a.length-1]}; }\r\nvar EXPORTS = { quartiles:quartiles, quantileSorted:quantileSorted, mean:mean };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.QuartileForgePure = EXPORTS; }\r\n\r\n"
}