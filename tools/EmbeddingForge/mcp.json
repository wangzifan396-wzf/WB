{
  "id": "EmbeddingForge",
  "name": "embedding_forge",
  "title": "EmbeddingForge",
  "description": "用确定性哈希生成词向量并算余弦相似度，演示语义接近度。",
  "entry": "vec",
  "entries": {
    "vec": {
      "paramStyle": "positional",
      "paramNames": [
        "word",
        "dim"
      ],
      "fields": []
    },
    "cosine": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "word": {
        "type": "string",
        "description": "第 1 个参数（word）"
      },
      "dim": {
        "type": "number",
        "description": "第 2 个参数（dim）（数值）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction vec(word, dim){\r\n  dim=dim||16; word=String(word); var v=[];\r\n  for(var d=0;d<dim;d++){ var s=0; for(var i=0;i<word.length;i++){ var c=word.charCodeAt(i); s+=Math.sin((c*(d+1)+i*7)*0.5)*1.0; } v.push(s); }\r\n  return v;\r\n}\r\nfunction cosine(a,b){\r\n  if(!a||!b) return 0;\r\n  var d=Math.min(a.length,b.length), dot=0, na=0, nb=0;\r\n  for(var i=0;i<d;i++){ dot+=a[i]*b[i]; na+=a[i]*a[i]; nb+=b[i]*b[i]; }\r\n  if(na===0||nb===0) return 0;\r\n  return dot/(Math.sqrt(na)*Math.sqrt(nb));\r\n}\r\nvar EXPORTS={vec:vec,cosine:cosine};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.EmbeddingForgePure = EXPORTS; }\r\n\r\n"
}