{
  "id": "UrlParseForge",
  "name": "url_parse_forge",
  "title": "UrlParseForge",
  "description": "URL 解析：拆分协议/主机/端口/路径/查询/锚点，并解码查询参数为键值对，离线可用。",
  "entry": "parse",
  "entries": {
    "parse": {
      "paramStyle": "single",
      "paramNames": [
        "url"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "输入（url）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction parse(url){\r\n  var mm=url.match(/^(([a-zA-Z]+:)\\/\\/)?([^:/?#]*)(:(\\d+))?([^?#]*)(\\?([^#]*))?(#(.*))?$/);\r\n  if(!mm) return null;\r\n  var proto=mm[2]?mm[2].slice(0,-1):''; var host=mm[3]||''; var port=mm[5]||'';\r\n  var pathname=mm[6]||''; var q=mm[8]||''; var hash=mm[10]||'';\r\n  var params={};\r\n  if(q){ q.split('&').forEach(function(pair){ if(!pair) return; var kv=pair.split('='); var key=decodeURIComponent(kv[0]); var val=kv[1]!==undefined?decodeURIComponent(kv[1]):''; params[key]=val; }); }\r\n  return {protocol:proto, host:host, port:port, pathname:pathname, query:q, hash:hash, params:params};\r\n}\r\nvar EXPORTS={parse:parse};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.UrlParseForgePure = EXPORTS; }\r\n\r\n"
}