{
  "id": "AspectRatioForge",
  "name": "aspect_ratio_forge",
  "title": "AspectRatioForge",
  "description": "宽高比化简（GCD）与等比缩放计算，内置常见预设（16:9/4:3/1:1 等），离线可用。",
  "entry": "gcd",
  "entries": {
    "gcd": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    },
    "simplify": {
      "paramStyle": "positional",
      "paramNames": [
        "w",
        "h"
      ],
      "fields": []
    },
    "scale": {
      "paramStyle": "positional",
      "paramNames": [
        "w",
        "h",
        "opt"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "a": {
        "type": "number",
        "description": "第 1 个参数（a）（数值）"
      },
      "b": {
        "type": "string",
        "description": "第 2 个参数（b）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction gcd(a,b){ a=Math.abs(a); b=Math.abs(b); while(b){ var t=b; b=a%b; a=t; } return a||1; }\r\nfunction simplify(w,h){ var g=gcd(w,h); return (w/g)+':'+(h/g); }\r\nfunction scale(w,h,opt){\r\n  // opt: {width} or {height} -> compute the other preserving ratio\r\n  if(opt.width!==undefined){ return Math.round(h*(opt.width/w)); }\r\n  if(opt.height!==undefined){ return Math.round(w*(opt.height/h)); }\r\n  return null;\r\n}\r\nvar PRESETS=[['16:9',16,9],['4:3',4,3],['3:2',3,2],['1:1',1,1],['21:9',21,9],['5:4',5,4],['2:3',2,3],['9:16',9,16]];\r\nvar EXPORTS={gcd:gcd, simplify:simplify, scale:scale, PRESETS:PRESETS};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.AspectRatioForgePure = EXPORTS; }\r\n\r\n"
}