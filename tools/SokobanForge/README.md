# SokobanForge

推箱子游戏。把每个箱子推到目标点上即可过关——经典仓库物流谜题，纯函数内核可断言移动、推动与胜利判定。

## 特性
- `skParse(level)` 解析字符关卡（# 墙 / @ 玩家 / $ 箱 / . 目标 / * 箱在目标上）
- `skMove(s, dir)` 推箱子（遇墙或双箱阻挡则不动；dir: 0上 1右 2下 3左）
- `skWin(s)` 全部箱子抵达目标即胜
- 三关内置、方向键操作、步数统计；纯前端、零依赖、离线

## 测试
```
node _test.js && node smoke.js
```
