{
  "id": "NormalDistForge",
  "name": "normal_dist_forge",
  "title": "NormalDistForge",
  "description": "正态分布：概率密度 PDF、累积分布 CDF、误差函数 erf 与分位数（逆 CDF）二分求解。",
  "entry": "erf",
  "entries": {
    "erf": {
      "paramStyle": "single",
      "paramNames": [
        "x"
      ],
      "fields": []
    },
    "pdf": {
      "paramStyle": "positional",
      "paramNames": [
        "x",
        "mu",
        "sigma"
      ],
      "fields": []
    },
    "cdf": {
      "paramStyle": "positional",
      "paramNames": [
        "x",
        "mu",
        "sigma"
      ],
      "fields": []
    },
    "quantile": {
      "paramStyle": "positional",
      "paramNames": [
        "p",
        "mu",
        "sigma"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "x": {
        "type": "string",
        "description": "输入（x）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction erf(x){ var s=x<0?-1:1; x=Math.abs(x); var t=1/(1+0.3275911*x); var y=1-(((((1.061405429*t-1.453152027)*t)+1.421413741)*t-0.284496736)*t+0.254829592)*t*Math.exp(-x*x); return s*y; }\r\nfunction pdf(x, mu, sigma){ return Math.exp(-0.5*Math.pow((x-mu)/sigma,2))/(sigma*Math.sqrt(2*Math.PI)); }\r\nfunction cdf(x, mu, sigma){ return 0.5*(1+erf((x-mu)/(sigma*Math.sqrt(2)))); }\r\nfunction quantile(p, mu, sigma){ var lo=-10, hi=10; for(var i=0;i<60;i++){ var m=(lo+hi)/2; if(cdf(m,0,1)<p) lo=m; else hi=m; } return mu+((lo+hi)/2)*sigma; }\r\nvar EXPORTS = { erf:erf, pdf:pdf, cdf:cdf, quantile:quantile };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.NormalDistForgePure = EXPORTS; }\r\n\r\n"
}