{
  "id": "VectorDotForge",
  "name": "vector_dot_forge",
  "title": "向量点积",
  "description": "计算等长向量的点积、模长与夹角，离线。",
  "entry": "dot",
  "entries": {
    "dot": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    },
    "magnitude": {
      "paramStyle": "single",
      "paramNames": [
        "v"
      ],
      "fields": []
    },
    "angleDeg": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "a": {
        "type": "string",
        "description": "第 1 个参数（a）"
      },
      "b": {
        "type": "string",
        "description": "第 2 个参数（b）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction dot(a,b){ if(!Array.isArray(a)||!Array.isArray(b)||a.length!==b.length||!a.length) return {error:'需等长非空向量'};\r\n  var s=0; for(var i=0;i<a.length;i++) s+=a[i]*b[i]; return Math.round(s*1000)/1000; }\r\nfunction magnitude(v){ if(!Array.isArray(v)||!v.length) return {error:'需向量'}; var s=0; for(var i=0;i<v.length;i++) s+=v[i]*v[i]; return Math.round(Math.sqrt(s)*1000)/1000; }\r\nfunction angleDeg(a,b){ if(!Array.isArray(a)||!Array.isArray(b)||a.length!==b.length) return {error:'需等长向量'};\r\n  var d=dot(a,b), ma=magnitude(a), mb=magnitude(b); if(ma===0||mb===0) return {error:'零向量无夹角'};\r\n  var c=d/(ma*mb); c=c<-1?-1:(c>1?1:c); return Math.round(Math.acos(c)*180/Math.PI*100)/100; }\r\nvar EXPORTS={dot:dot, magnitude:magnitude, angleDeg:angleDeg};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.VectorDotForgePure = EXPORTS; }\r\n\r\n"
}