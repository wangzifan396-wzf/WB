{
  "id": "ModuloForge",
  "name": "modulo_forge",
  "title": "ModuloForge",
  "description": "模运算：取模（支持负数）、模逆（扩展欧几里得）与模幂（快速幂），BigInt 精确。",
  "entry": "mod",
  "entries": {
    "mod": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "m"
      ],
      "fields": []
    },
    "modInverse": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "m"
      ],
      "fields": []
    },
    "modPow": {
      "paramStyle": "positional",
      "paramNames": [
        "b",
        "e",
        "m"
      ],
      "fields": []
    },
    "egcd": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "a": {
        "type": "string",
        "description": "第 1 个参数（a）"
      },
      "m": {
        "type": "string",
        "description": "第 2 个参数（m）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction mod(a, m){ a=BigInt(a); m=BigInt(m); if(m<=0n) return NaN; a%=m; if(a<0n) a+=m; return a; }\r\nfunction egcd(a, b){ a=BigInt(a); b=BigInt(b); if(b===0n) return [a,1n,0n]; var r=egcd(b, a%b); var g=r[0], x=r[2], y=r[1]-((a/b)*r[2]); return [g, x, y]; }\r\nfunction modInverse(a, m){ m=BigInt(m); if(m<=0n) return null; a=mod(a, m); var r=egcd(a, m); if(r[0]!==1n) return null; var x=r[1]%m; if(x<0n) x+=m; return x; }\r\nfunction modPow(b, e, m){ m=BigInt(m); b=mod(b,m); e=BigInt(e); if(e<0n) return null; var r=1n; while(e>0n){ if(e&1n) r=(r*b)%m; e>>=1n; b=(b*b)%m; } return r; }\r\nvar EXPORTS = { mod:mod, modInverse:modInverse, modPow:modPow, egcd:egcd };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ModuloForgePure = EXPORTS; }\r\n\r\n"
}