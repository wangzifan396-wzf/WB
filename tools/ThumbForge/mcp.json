{
  "id": "ThumbForge",
  "name": "thumb_forge",
  "title": "ThumbForge",
  "description": "客户端 canvas 生成缩略图，按最长边等比缩放，导出 PNG。",
  "entry": "thumbDims",
  "entries": {
    "thumbDims": {
      "paramStyle": "positional",
      "paramNames": [
        "w",
        "h",
        "maxDim"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "w": {
        "type": "string",
        "description": "第 1 个参数（w）"
      },
      "h": {
        "type": "string",
        "description": "第 2 个参数（h）"
      },
      "maxDim": {
        "type": "string",
        "description": "第 3 个参数（maxDim）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction thumbDims(w, h, maxDim){\r\n  if(!(w>0)||!(h>0)||!(maxDim>0)) return null;\r\n  var L=Math.max(w,h);\r\n  if(L<=maxDim) return {w:Math.round(w),h:Math.round(h)};\r\n  var f=maxDim/L;\r\n  return {w:Math.round(w*f),h:Math.round(h*f)};\r\n}\r\nvar EXPORTS={thumbDims:thumbDims};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ThumbForgePure = EXPORTS; }\r\n\r\n"
}