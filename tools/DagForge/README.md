# DagForge

DAG 工作流分析器。用 `task=工期` + `A -> B` 依赖边描述流水线，一键得到确定性拓扑执行顺序、关键路径与总工期。

## 特性
- Kahn 拓扑排序（字典序稳定输出）+ 环检测
- 关键路径法（CPM）：最早完成时间递推 + 路径回溯
- 纯前端、零依赖、离线可用

## 测试
```
node _test.js && node smoke.js
```
