# CronForge

离线 Cron 表达式解析器 · 单文件 · 零依赖 · 本地优先。

## 功能
- 5 字段解析（分 时 日 月 周），支持 `*`、`,`、`-`、`/`、月份/星期名称（JAN-DEC、SUN-SAT）
- 计算「下次运行时间」与未来 N 次命中时间（标准 POSIX 语义：日/周同时为限定条件时取「或」）
- 人类可读化描述（如「每周周一至周五 每天 09:30」）

纯函数：`parse` / `fits` / `next` / `nextN` / `describe` —— 见 `_test.js`。
