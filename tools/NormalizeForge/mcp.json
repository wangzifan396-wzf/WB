{
  "id": "NormalizeForge",
  "name": "normalize_forge",
  "title": "NormalizeForge",
  "description": "peak / rms 两种音量归一化并导出 WAV，离线。",
  "entry": "normalize",
  "entries": {
    "normalize": {
      "paramStyle": "positional",
      "paramNames": [
        "samples",
        "mode",
        "target"
      ],
      "fields": []
    },
    "wavEncode": {
      "paramStyle": "positional",
      "paramNames": [
        "samples",
        "sr"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "samples": {
        "type": "string",
        "description": "第 1 个参数（samples）"
      },
      "mode": {
        "type": "string",
        "description": "第 2 个参数（mode）"
      },
      "target": {
        "type": "string",
        "description": "第 3 个参数（target）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction normalize(samples, mode, target){ if(!samples||!samples.length) return [];\r\n  target=target==null?0.95:target; mode=mode||'peak'; var n=samples.length, i, m=0;\r\n  if(mode==='rms'){ var s2=0; for(i=0;i<n;i++) s2+=samples[i]*samples[i]; var r=Math.sqrt(s2/n);\r\n    if(r===0) return samples.slice(); var f=target/r; var o=new Array(n); for(i=0;i<n;i++) o[i]=samples[i]*f; return o; }\r\n  for(i=0;i<n;i++) m=Math.max(m,Math.abs(samples[i])); if(m===0) return samples.slice();\r\n  var f=target/m; var o=new Array(n); for(i=0;i<n;i++) o[i]=samples[i]*f; return o;\r\n}\r\nfunction wavEncode(samples, sr){ sr=sr||44100; var n=samples.length; var buf=new ArrayBuffer(44+n*2); var dv=new DataView(buf);\r\n  function ws(o,s){for(var i=0;i<s.length;i++) dv.setUint8(o+i,s.charCodeAt(i));}\r\n  ws(0,'RIFF'); dv.setUint32(4,36+n*2,true); ws(8,'WAVE'); ws(12,'fmt '); dv.setUint32(16,16,true);\r\n  dv.setUint16(20,1,true); dv.setUint16(22,1,true); dv.setUint32(24,sr,true); dv.setUint32(28,sr*2,true);\r\n  dv.setUint16(32,2,true); dv.setUint16(34,16,true); ws(36,'data'); dv.setUint32(40,n*2,true);\r\n  var o=44; for(var i=0;i<n;i++){ var s=Math.max(-1,Math.min(1,samples[i])); dv.setInt16(o, s<0?s*0x8000:s*0x7FFF, true); o+=2; }\r\n  return buf;\r\n}\r\nvar EXPORTS={normalize:normalize,wavEncode:wavEncode};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.NormalizeForgePure = EXPORTS; }\r\n\r\n"
}