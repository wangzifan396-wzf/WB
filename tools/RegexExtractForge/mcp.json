{
  "id": "RegexExtractForge",
  "name": "regex_extract_forge",
  "title": "正则提取",
  "description": "正则提取：自定义正则或内置预设（邮箱/URL/IPv4/日期/手机号/数字）抽取匹配项，离线处理。",
  "entry": "regexExtract",
  "entries": {
    "regexExtract": {
      "paramStyle": "positional",
      "paramNames": [
        "text",
        "opts"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "第 1 个参数（text）"
      },
      "opts": {
        "type": "object",
        "description": "第 2 个参数（opts）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar REGEX_PRESETS={\r\n  email:'[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\\\.[A-Za-z]{2,}',\r\n  url:'https?://[^\\\\s]+',\r\n  ipv4:'\\\\b(?:\\\\d{1,3}\\\\.){3}\\\\d{1,3}\\\\b',\r\n  date:'\\\\d{4}-\\\\d{2}-\\\\d{2}',\r\n  cnPhone:'1[3-9]\\\\d{9}',\r\n  number:'-?\\\\d+(?:\\\\.\\\\d+)?'\r\n};\r\nfunction regexExtract(text, opts){\r\n  opts=opts||{}; var pat=opts.pattern||''; var flags=opts.flags||'g';\r\n  if(flags.indexOf('g')<0) flags+='g';\r\n  var re;\r\n  try { re=new RegExp(pat, flags); } catch(e){ return {ok:false, reason:'invalid regex', matches:[], count:0}; }\r\n  var t=String(text||''); var m, out=[]; var limit=opts.limit!=null?opts.limit:1000;\r\n  while((m=re.exec(t))!==null){\r\n    out.push(m[0]);\r\n    if(m[0]==='') re.lastIndex++;\r\n    if(out.length>=limit) break;\r\n  }\r\n  return {ok:true, matches:out, count:out.length};\r\n}\r\n\r\nvar EXPORTS={regexExtract:regexExtract,REGEX_PRESETS:REGEX_PRESETS};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.RegexExtractForgePure = EXPORTS; }\r\n\r\n"
}