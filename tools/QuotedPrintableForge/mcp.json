{
  "id": "QuotedPrintableForge",
  "name": "quoted_printable_forge",
  "title": "QuotedPrintableForge",
  "description": "Quoted-Printable 编码 / 解码（RFC 2045）：把非 ASCII 与特殊字符编码成 =XX 形式，是电子邮件正文的标准编码之一。支持编码与解码，纯本地、零依赖。",
  "entry": "qpEncStr",
  "entries": {
    "qpEncStr": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "qpDecStr": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction qpEncStr(s){ var out=\"\"; for(var i=0;i<s.length;i++){ var c=s.charCodeAt(i); if(c===0x3d){ out+='=3D'; } else if(c===0x09||c===0x0a||c===0x0d){ out+=s[i]; } else if(c<0x20||c>0x7e){ out+='='+('0'+c.toString(16).toUpperCase()).slice(-2); } else { out+=s[i]; } } return out; }\r\nfunction qpDecStr(s){ s=s.replace(/=\\r?\\n/g,''); return s.replace(/=([0-9A-Fa-f]{2})/g, function(_,h){ return String.fromCharCode(parseInt(h,16)); }); }\r\nvar EXPORTS={qpEncStr:qpEncStr, qpDecStr:qpDecStr};\r\n\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.QuotedPrintableForgePure = EXPORTS; }\r\n\r\n"
}