# RegexRailForge

正则铁路图（railroad diagram）生成器。把正则表达式解析为轨道图：拼接是横向串联、选择是纵向分支、量词是回环弧线，语法结构一眼看清。

## 支持
- 字面量 / `.` / 转义 `\d` / 字符类 `[a-z]`
- 分组 `()`、量词 `* + ?`、选择 `|`

## 特性
- 纯前端解析 + 布局，实时渲染
- 零依赖、离线可用

## 测试
```
node _test.js && node smoke.js
```
