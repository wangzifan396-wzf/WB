{
  "id": "DpiForge",
  "name": "dpi_forge",
  "title": "DpiForge",
  "description": "PPI / DPI 计算工具：由屏幕对角线英寸与分辨率算出像素密度（PPI），并换算每英寸/毫米对应像素，判断屏幕与图片清晰度。",
  "entry": "ppi",
  "entries": {
    "ppi": {
      "paramStyle": "positional",
      "paramNames": [
        "diag",
        "wpx",
        "hpx"
      ],
      "fields": []
    },
    "pxFromInch": {
      "paramStyle": "positional",
      "paramNames": [
        "p",
        "inch"
      ],
      "fields": []
    },
    "dpmm": {
      "paramStyle": "single",
      "paramNames": [
        "p"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "diag": {
        "type": "string",
        "description": "第 1 个参数（diag）"
      },
      "wpx": {
        "type": "string",
        "description": "第 2 个参数（wpx）"
      },
      "hpx": {
        "type": "string",
        "description": "第 3 个参数（hpx）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\n(function(){\r\n  function ppi(diag,wpx,hpx){if(!diag||!wpx||!hpx)return{error:'对角线与分辨率均需为正'};var d=Math.sqrt(wpx*wpx+hpx*hpx)/diag;return {value:Math.round(d*100)/100};}\r\n  function pxFromInch(p,inch){return Math.round(p*inch);}\r\n  function dpmm(p){return Math.round(p/25.4*100)/100;}\r\n  var API={ppi:ppi,pxFromInch:pxFromInch,dpmm:dpmm};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__DpiForge__=API;\r\n})();\r\n\r\n"
}