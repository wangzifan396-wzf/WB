{
  "id": "ResizeForge",
  "name": "resize_forge",
  "title": "ResizeForge",
  "description": "客户端 canvas 缩放图片，支持按比例/定宽/定高/最长边，导出 PNG。",
  "entry": "computeDims",
  "entries": {
    "computeDims": {
      "paramStyle": "positional",
      "paramNames": [
        "w",
        "h",
        "mode",
        "val"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "w": {
        "type": "string",
        "description": "第 1 个参数（w）"
      },
      "h": {
        "type": "string",
        "description": "第 2 个参数（h）"
      },
      "mode": {
        "type": "string",
        "description": "第 3 个参数（mode）"
      },
      "val": {
        "type": "number",
        "description": "第 4 个参数（val）（数值）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction computeDims(w,h,mode,val){\r\n  if(!(w>0)||!(h>0)) return null;\r\n  if(mode==='scale'){ var f=(val||100)/100; return {w:Math.round(w*f),h:Math.round(h*f)}; }\r\n  if(mode==='width'){ var nw=Math.max(1,Math.round(val)); return {w:nw,h:Math.round(h*nw/w)}; }\r\n  if(mode==='height'){ var nh=Math.max(1,Math.round(val)); return {w:Math.round(w*nh/h),h:nh}; }\r\n  if(mode==='longest'){ var L=Math.max(1,Math.round(val)); return (w>=h)?{w:L,h:Math.round(h*L/w)}:{w:Math.round(w*L/h),h:L}; }\r\n  return {w:w,h:h};\r\n}\r\nvar EXPORTS={computeDims:computeDims};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.ResizeForgePure = EXPORTS; }\r\n\r\n"
}