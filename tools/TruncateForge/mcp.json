{
  "id": "TruncateForge",
  "name": "truncate_forge",
  "title": "文本截断",
  "description": "文本截断：按字符/词/显示宽度（CJK 计 2 宽）截断并追加可自定义省略号，离线处理。",
  "entry": "truncateText",
  "entries": {
    "truncateText": {
      "paramStyle": "positional",
      "paramNames": [
        "text",
        "opts"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "第 1 个参数（text）"
      },
      "opts": {
        "type": "object",
        "description": "第 2 个参数（opts）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction truncateText(text, opts){\r\n  opts=opts||{}; var n=opts.n!=null?opts.n:100; if(n<0)n=0;\r\n  var ell=opts.ellipsis!=null?opts.ellipsis:'…'; var mode=opts.mode||'char';\r\n  var t=String(text||'');\r\n  if(mode==='char'){ if(t.length<=n) return t; return t.slice(0,n)+ell; }\r\n  if(mode==='word'){ var ws=t.split(/\\s+/).filter(function(x){return x.length;}); if(ws.length<=n) return t; return ws.slice(0,n).join(' ')+ell; }\r\n  var w=0, out='';\r\n  for(var i=0;i<t.length;i++){ var cw=(t.charCodeAt(i)>0x2E80)?2:1; if(w+cw>n) return out+ell; out+=t.charAt(i); w+=cw; }\r\n  return t;\r\n}\r\n\r\nvar EXPORTS={truncateText:truncateText};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.TruncateForgePure = EXPORTS; }\r\n\r\n"
}