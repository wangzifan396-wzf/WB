{
  "id": "TitleForge",
  "name": "title_forge",
  "title": "TitleForge",
  "description": "标题大小写与命名风格一键转换：Title Case、Sentence case、UPPER、lower、kebab-case、snake_case、camelCase、PascalCase，适合 SEO 与代码命名。",
  "entry": "variants",
  "entries": {
    "variants": {
      "paramStyle": "single",
      "paramNames": [
        "t"
      ],
      "fields": []
    },
    "titleCase": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "kebab": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "t": {
        "type": "string",
        "description": "输入（t）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\n(function(){\r\n  var SMALL=['the','a','an','and','or','but','of','to','in','on','for','with','at','by','from','as'];\r\n  function cap(w){return w.charAt(0).toUpperCase()+w.slice(1).toLowerCase();}\r\n  function titleCase(s){var w=s.split(/\\s+/);return w.map(function(x,i){if(i===0)return cap(x);return SMALL.indexOf(x.toLowerCase())>=0?x.toLowerCase():cap(x);}).join(' ');}\r\n  function sentenceCase(s){var w=s.split(/\\s+/);return w.map(function(x,i){return i===0?cap(x):x.toLowerCase();}).join(' ');}\r\n  function kebab(s){return s.toLowerCase().replace(/[^a-z0-9一-鿿]+/g,'-').replace(/^-+|-+$/g,'');}\r\n  function snake(s){return s.toLowerCase().replace(/[^a-z0-9一-鿿]+/g,'_').replace(/^_+|_+$/g,'');}\r\n  function camel(s){var p=kebab(s).split('-').filter(Boolean);return p.map(function(x,i){return i===0?x:cap(x);}).join('');}\r\n  function pascal(s){return camel(s).charAt(0).toUpperCase()+camel(s).slice(1);}\r\n  function variants(t){t=String(t);return {titleCase:titleCase(t),sentenceCase:sentenceCase(t),upper:t.toUpperCase(),lower:t.toLowerCase(),kebab:kebab(t),snake:snake(t),camel:camel(t),pascal:pascal(t)};}\r\n  var API={variants:variants,titleCase:titleCase,kebab:kebab};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__TitleForge__=API;\r\n})();\r\n\r\n"
}