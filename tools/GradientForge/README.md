# GradientForge

离线 CSS 渐变可视化构建器 · 单文件 · 零依赖 · 本地优先。

## 功能
- 线性 / 径向 / 锥形三种渐变
- 任意数量色标（颜色 + 位置），可增删
- 实时预览 + 角度调节
- 一键复制标准 CSS `background`

## 纯函数
`buildGradient` / `buildStops` / `clampPct` / `toBackground` —— 见 `_test.js`。
