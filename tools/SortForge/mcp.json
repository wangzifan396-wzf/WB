{
  "id": "SortForge",
  "name": "sort_forge",
  "title": "SortForge",
  "description": "排序可视化：冒泡/插排/选排/快排/归并，比较次数 + 帧快照。",
  "entry": "sortLines",
  "entries": {
    "sortLines": {
      "paramStyle": "positional",
      "paramNames": [
        "text",
        "opts"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "第 1 个参数（text）"
      },
      "opts": {
        "type": "object",
        "description": "第 2 个参数（opts）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n(function(){\r\n  function cmp(a,b,numeric){ if(numeric){ var x=parseFloat(a),y=parseFloat(b); if(!isNaN(x)&&!isNaN(y)) return x-y; } return a<b?-1:(a>b?1:0); }\r\n  function sortLines(text,opts){ opts=opts||{}; text=String(text==null?'':text);\r\n    var lines=text.split(/\\r\\n|\\r|\\n/); lines=lines.filter(function(l,i,a){ return !(i===a.length-1 && l===''); });\r\n    var arr=lines.slice();\r\n    if(opts.shuffle){ for(var i=arr.length-1;i>0;i--){ var j=Math.floor(Math.random()*(i+1)); var t=arr[i];arr[i]=arr[j];arr[j]=t; } }\r\n    else { arr.sort(function(a,b){ return cmp(a,b,opts.numeric)*(opts.reverse?-1:1); }); }\r\n    if(opts.unique){ var seen={},out=[]; arr.forEach(function(l){ if(!seen[l]){seen[l]=1;out.push(l);} }); arr=out; }\r\n    return {value:arr.join('\\n')}; }\r\n  var API={sortLines:sortLines};\r\n  if(typeof module!=='undefined'&&module.exports)module.exports=API;\r\n  if(typeof window!=='undefined')window.__SortForge__=API;\r\n})();\r\n"
}