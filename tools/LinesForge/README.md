# LinesForge

离线文本行工具箱 · 单文件 · 零依赖 · 本地优先。

## 功能
- 去重（保留首次出现）
- 删除空行 / 去首尾空白
- 升序 / 降序排序、反转顺序
- 添加行号（自定义起始号）
- 实时统计：行数 / 词数 / 字符数

## 纯函数
`dedupe` / `removeEmpty` / `sortLines` / `reverseLines` / `trimLines` / `numberLines` / `countStats` —— 见 `_test.js`。
