# TetrisForge

俄罗斯方块。纯逻辑内核（碰撞检测、旋转、消行、锁定、硬降）与 Canvas 游戏界面，方向键即可游玩。

## 操作
- ← → 移动，↑ 旋转，↓ 软降，空格 硬降

## 特性
- 内核为可测试的纯函数，UI 仅负责渲染与输入
- 7 种标准方块、下一个预览、计分与消行
- 纯前端、零依赖、离线

## 测试
```
node _test.js && node smoke.js
```
