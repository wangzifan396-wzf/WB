{
  "id": "RandomColorForge",
  "name": "random_color_forge",
  "title": "RandomColorForge",
  "description": "生成随机且对比度达标的配色方案",
  "entry": "hslToHex",
  "entries": {
    "hslToHex": {
      "paramStyle": "positional",
      "paramNames": [
        "h",
        "s",
        "l"
      ],
      "fields": []
    },
    "lum": {
      "paramStyle": "single",
      "paramNames": [
        "hex"
      ],
      "fields": []
    },
    "contrast": {
      "paramStyle": "positional",
      "paramNames": [
        "h1",
        "h2"
      ],
      "fields": []
    },
    "palette": {
      "paramStyle": "positional",
      "paramNames": [
        "n",
        "rng",
        "minContrast"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "h": {
        "type": "string",
        "description": "第 1 个参数（h）"
      },
      "s": {
        "type": "string",
        "description": "第 2 个参数（s）"
      },
      "l": {
        "type": "string",
        "description": "第 3 个参数（l）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction hslToHex(h,s,l){ h=(h%360+360)%360; s=Math.max(0,Math.min(100,s))/100; l=Math.max(0,Math.min(100,l))/100;\r\n  var c=(1-Math.abs(2*l-1))*s, x=c*(1-Math.abs((h/60)%2-1)), mm=l-c/2, r,g,b;\r\n  if(h<60){r=c;g=x;b=0;} else if(h<120){r=x;g=c;b=0;} else if(h<180){r=0;g=c;b=x;}\r\n  else if(h<240){r=0;g=x;b=c;} else if(h<300){r=x;g=0;b=c;} else {r=c;g=0;b=x;}\r\n  var to=function(v){var n=Math.round((v+mm)*255);return ('0'+n.toString(16)).slice(-2);};\r\n  return '#'+to(r)+to(g)+to(b); }\r\nfunction lum(hex){ var r=parseInt(hex.slice(1,3),16)/255,g=parseInt(hex.slice(3,5),16)/255,b=parseInt(hex.slice(5,7),16)/255;\r\n  function f(c){return c<=0.03928?c/12.92:Math.pow((c+0.055)/1.055,2.4);} return 0.2126*f(r)+0.7152*f(g)+0.0722*f(b); }\r\nfunction contrast(h1,h2){ var l1=lum(h1),l2=lum(h2); var a=Math.max(l1,l2),b=Math.min(l1,l2); return (a+0.05)/(b+0.05); }\r\nfunction palette(n, rng, minContrast){ rng=rng||Math.random; n=+n||5; minContrast=+minContrast||0; var out=[];\r\n  for(var i=0;i<n;i++){ var hex; var tries=0; do { hex=hslToHex(rng()*360, 60+rng()*35, 30+rng()*45); tries++; } while(contrast(hex,'#ffffff')<minContrast && tries<20);\r\n    out.push({hex:hex, hsl:[Math.round(rng()*360),Math.round(60+rng()*35),Math.round(30+rng()*45)], onWhite:contrast(hex,'#ffffff')}); }\r\n  return out; }\r\nvar EXPORTS={hslToHex:hslToHex, lum:lum, contrast:contrast, palette:palette};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.RandomColorForgePure = EXPORTS; }\r\n\r\n"
}