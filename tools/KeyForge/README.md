# KeyForge

离线键盘按键侦测 · 单文件 · 零依赖 · 本地优先。

## 功能
- 实时显示 `event.key` / `event.code` / `keyCode` / 修饰键组合
- keyCode 数字反查按键名（内置 100+ 键码表）
- 一键复制按键信息 JSON

纯函数：`lookupKeyCode` / `describeKey` —— 见 `_test.js`。
