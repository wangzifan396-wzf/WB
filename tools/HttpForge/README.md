# HttpForge

离线 HTTP 速查手册 · 单文件 · 零依赖 · 本地优先。

## 功能
- HTTP 状态码速查（1xx-5xx，含分类，支持按码 / 名称搜索）
- 请求方法特性表（安全 / 幂等 / 请求体）
- 常见 MIME 类型查询（按扩展名）

纯函数：`lookupStatus` / `searchStatus` / `statusCategory` / `lookupMime` / `searchMime` —— 见 `_test.js`。
