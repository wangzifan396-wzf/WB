{
  "id": "EmbedForge",
  "name": "embed_forge",
  "title": "EmbedForge",
  "description": "以词袋向量演示两段文本的余弦相似度（AI 入门）。",
  "entry": "tokenize",
  "entries": {
    "tokenize": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "vec": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "cosine": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction tokenize(s){ return ((s||'').toLowerCase().match(/[a-z0-9\\u4e00-\\u9fa5]+/g))||[]; }\r\nfunction vec(s){ var t=tokenize(s), m={}; t.forEach(function(w){ m[w]=(m[w]||0)+1; }); return m; }\r\nfunction cosine(a,b){\r\n  var va=vec(a), vb=vec(b), keys={}, dot=0, na=0, nb=0;\r\n  for(var k in va){ keys[k]=1; na+=va[k]*va[k]; }\r\n  for(var k in vb){ keys[k]=1; nb+=vb[k]*vb[k]; }\r\n  for(var k in keys){ var x=va[k]||0, y=vb[k]||0; dot+=x*y; }\r\n  if(na===0||nb===0) return 0;\r\n  return dot/(Math.sqrt(na)*Math.sqrt(nb));\r\n}\r\nvar EXPORTS={tokenize:tokenize,vec:vec,cosine:cosine};\r\n\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.EmbedForgePure = EXPORTS; }\r\n\r\n"
}