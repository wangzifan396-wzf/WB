{
  "id": "BioForge",
  "name": "bio_forge",
  "title": "BioForge",
  "description": "按身份与兴趣生成多风格社交简介文案",
  "entry": "generate",
  "entries": {
    "generate": {
      "paramStyle": "object",
      "paramNames": [
        "o"
      ],
      "fields": [
        "name",
        "role",
        "interests",
        "tone",
        "emoji"
      ]
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "参数 name"
      },
      "role": {
        "type": "string",
        "description": "参数 role"
      },
      "interests": {
        "type": "string",
        "description": "参数 interests"
      },
      "tone": {
        "type": "string",
        "description": "参数 tone"
      },
      "emoji": {
        "type": "string",
        "description": "参数 emoji"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction generate(o){ o=o||{}; var name=o.name||'Your Name', role=o.role||'professional', interests=o.interests||'what you love', tone=o.tone||'pro', emoji=o.emoji||'✨';\r\n  if(tone==='casual') return [ name+' — '+role+' who loves '+interests+'. '+emoji, 'Just '+name+', '+role+'. Into '+interests+' '+emoji, name+': '+role+' · '+interests+' enthusiast '+emoji ];\r\n  if(tone==='funny') return [ name+' | '+role+' (probably) | '+interests+' fanatic '+emoji, 'Hi, I am '+name+', a '+role+' who will not stop talking about '+interests, name+': '+role+', '+interests+' connoisseur. Send coffee. '+emoji ];\r\n  return [ name+' · '+role+'. Helping '+interests+' thrive.', name+' — '+role+' focused on '+interests+'.', name+': '+role+' passionate about '+interests+'.' ]; }\r\nvar EXPORTS={generate:generate};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.BioForgePure = EXPORTS; }\r\n\r\n"
}