{
  "id": "HashtagForge",
  "name": "hashtag_forge",
  "title": "HashtagForge",
  "description": "话题标签生成：由主题词生成一组话题标签变体，用于社媒发布，离线随机模板。",
  "entry": "makeTag",
  "entries": {
    "makeTag": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    },
    "generate": {
      "paramStyle": "positional",
      "paramNames": [
        "topic",
        "count",
        "style"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "s": {
        "type": "string",
        "description": "输入（s）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar PRE = ['','OfTheDay','Lovers','Life','Time','World','Daily','Official','HQ','2026'];\r\nvar STYLE = ['best','top','my','the','real','cool','pro','new'];\r\nfunction makeTag(s){ if(!s) return ''; return String(s).trim().replace(/[^A-Za-z0-9一-龥]/g,''); }\r\nfunction generate(topic, count, style){ topic=makeTag(topic); if(!topic) return []; count=count||6; style=style||'presets'; var out=[]; out.push('#'+topic); for(var i=0;i<PRE.length && out.length<count;i++){ out.push('#'+topic+PRE[i]); } var k=0; while(out.length<count){ out.push('#'+topic+STYLE[k%STYLE.length]+((k/STYLE.length)|0)); k++; if(k>200) break; } return out.slice(0,count); }\r\nvar EXPORTS={ makeTag:makeTag, generate:generate, PRE:PRE, STYLE:STYLE };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.HashtagForgePure = EXPORTS; }\r\n\r\n"
}