{
  "id": "CombinationForge",
  "name": "combination_forge",
  "title": "CombinationForge",
  "description": "组合数 C(n,k)（BigInt 乘法公式，精确无溢出），并可展开二项式整行系数。",
  "entry": "nCr",
  "entries": {
    "nCr": {
      "paramStyle": "positional",
      "paramNames": [
        "n",
        "k"
      ],
      "fields": []
    },
    "nCrList": {
      "paramStyle": "single",
      "paramNames": [
        "n"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "n": {
        "type": "string",
        "description": "第 1 个参数（n）"
      },
      "k": {
        "type": "string",
        "description": "第 2 个参数（k）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction nCr(n, k){\r\n  n = BigInt(n); k = BigInt(k);\r\n  if(k < 0n || k > n) return null;\r\n  if(k > n - k) k = n - k;\r\n  var r = 1n;\r\n  for(var i = 0n; i < k; i++){ r = r * (n - i) / (i + 1n); }\r\n  return r;\r\n}\r\nfunction nCrList(n){ n=BigInt(n); var out=[]; for(var k=0n;k<=n;k++) out.push(nCr(n,k)); return out; }\r\nvar EXPORTS = { nCr:nCr, nCrList:nCrList };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.CombinationForgePure = EXPORTS; }\r\n\r\n"
}