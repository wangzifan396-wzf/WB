{
  "id": "KaleidoscopeForge",
  "name": "kaleidoscope_forge",
  "title": "KaleidoscopeForge",
  "description": "参数化生成对称万花筒线纹图案",
  "entry": "kGenerate",
  "entries": {
    "kGenerate": {
      "paramStyle": "object",
      "paramNames": [
        "opts"
      ],
      "fields": [
        "segments",
        "symmetry",
        "seed"
      ]
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "segments": {
        "type": "number",
        "description": "参数 segments（数值）"
      },
      "symmetry": {
        "type": "number",
        "description": "参数 symmetry（数值）"
      },
      "seed": {
        "type": "number",
        "description": "参数 seed（数值）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction rngFactory(seed){ var s=(seed>>>0)||1; return function(){ s=(s*1664525+1013904223)>>>0; return s/4294967296; }; }\r\nfunction kGenerate(opts){ opts=opts||{}; var segs=opts.segments||6, sym=opts.symmetry||8, seed=opts.seed||1;\r\n  var rng=rngFactory(seed); var lines=[]; var wedge=Math.PI*2/sym;\r\n  for(var i=0;i<segs;i++){\r\n    var a1=rng()*wedge, a2=rng()*wedge;\r\n    var x1=rng()*0.42, y1=rng()*0.42, x2=rng()*0.46, y2=rng()*0.46;\r\n    var p1x=Math.cos(a1)*x1, p1y=Math.sin(a1)*x1, p2x=Math.cos(a2)*x2, p2y=Math.sin(a2)*x2;\r\n    for(var s=0;s<sym;s++){ var ang=s*wedge, ca=Math.cos(ang), sa=Math.sin(ang);\r\n      lines.push({x1:+(p1x*ca-p1y*sa).toFixed(4), y1:+(p1x*sa+p1y*ca).toFixed(4), x2:+(p2x*ca-p2y*sa).toFixed(4), y2:+(p2x*sa+p2y*ca).toFixed(4)}); }\r\n  }\r\n  return {lines:lines, segments:segs, symmetry:sym, seed:seed}; }\r\nvar EXPORTS={kGenerate:kGenerate};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.KaleidoscopeForgePure = EXPORTS; }\r\n\r\n"
}