{
  "id": "CRTCForge",
  "name": "crtc_forge",
  "title": "CRT 显示器",
  "description": "扫描线 + 磷光微辉 + RGB 色散 + 暗角，模拟 CRT 显示器质感，扫描线/色散可调，离线处理。",
  "entry": "crtProcess",
  "entries": {
    "crtProcess": {
      "paramStyle": "positional",
      "paramNames": [
        "data",
        "w",
        "h",
        "opts"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "data": {
        "type": "string",
        "description": "第 1 个参数（data）"
      },
      "w": {
        "type": "string",
        "description": "第 2 个参数（w）"
      },
      "h": {
        "type": "string",
        "description": "第 3 个参数（h）"
      },
      "opts": {
        "type": "object",
        "description": "第 4 个参数（opts）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction crtProcess(data,w,h,opts){\r\n  opts=opts||{};\r\n  var scan=opts.scan!=null?opts.scan:0.35;\r\n  var ab=opts.ab!=null?opts.ab:2;\r\n  if(scan<0)scan=0; else if(scan>0.8)scan=0.8;\r\n  if(ab<0)ab=0; else if(ab>6)ab=6;\r\n  var out=new Uint8ClampedArray(data.length);\r\n  for(var y=0;y<h;y++){\r\n    var mul=(y%2===0)?(1-scan*0.5):1;\r\n    for(var x=0;x<w;x++){\r\n      var p=(y*w+x)*4;\r\n      var xr=Math.max(0,Math.min(w-1,x-ab)), xb=Math.max(0,Math.min(w-1,x+ab));\r\n      out[p]=data[(y*w+xr)*4]*mul; out[p+1]=data[p+1]*mul; out[p+2]=data[(y*w+xb)*4+2]*mul; out[p+3]=data[p+3];\r\n    }\r\n  }\r\n  return out;\r\n}\r\n\r\nvar EXPORTS={crtProcess:crtProcess};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.CRTCForgePure = EXPORTS; }\r\n\r\n"
}