{
  "id": "GcdLcmForge",
  "name": "gcd_lcm_forge",
  "title": "GcdLcmForge",
  "description": "最大公约数（GCD）与最小公倍数（LCM），支持多个整数，欧几里得算法 BigInt 精确无溢出。",
  "entry": "gcd",
  "entries": {
    "gcd": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    },
    "lcm": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    },
    "gcdList": {
      "paramStyle": "single",
      "paramNames": [
        "arr"
      ],
      "fields": []
    },
    "lcmList": {
      "paramStyle": "single",
      "paramNames": [
        "arr"
      ],
      "fields": []
    },
    "parseNums": {
      "paramStyle": "single",
      "paramNames": [
        "s"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "a": {
        "type": "string",
        "description": "第 1 个参数（a）"
      },
      "b": {
        "type": "string",
        "description": "第 2 个参数（b）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction parseNums(s){ return String(s).split(/[\\s,;]+/).map(function(x){return x.replace(/[^\\d-]/g,'');}).filter(function(x){return x.length && !isNaN(Number(x));}).map(Number).filter(function(x){return x!==0;}); }\r\nfunction gcd(a, b){ a=BigInt(a); b=BigInt(b); if(b<0n) b=-b; if(a<0n) a=-b?a:-a; while(b!==0n){ var t=a%b; a=b; b=t; } return a<0n?-a:a; }\r\nfunction lcm(a, b){ a=BigInt(a); b=BigInt(b); if(a===0n||b===0n) return 0n; return (a<0n?-a:a)/(gcd(a,b))*b; }\r\nfunction gcdList(arr){ if(!arr.length) return null; var g=BigInt(arr[0]); for(var i=1;i<arr.length;i++) g=gcd(g,arr[i]); return g<0n?-g:g; }\r\nfunction lcmList(arr){ if(!arr.length) return null; var l=BigInt(arr[0]); for(var i=1;i<arr.length;i++) l=lcm(l,arr[i]); return l; }\r\nvar EXPORTS = { gcd:gcd, lcm:lcm, gcdList:gcdList, lcmList:lcmList, parseNums:parseNums };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.GcdLcmForgePure = EXPORTS; }\r\n\r\n"
}