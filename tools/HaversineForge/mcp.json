{
  "id": "HaversineForge",
  "name": "haversine_forge",
  "title": "HaversineForge",
  "description": "大圆距离（Haversine）：两经纬度间球面最短距离，支持 km/mi/m，离线可用。",
  "entry": "haversine",
  "entries": {
    "haversine": {
      "paramStyle": "positional",
      "paramNames": [
        "lat1",
        "lon1",
        "lat2",
        "lon2",
        "unit"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "lat1": {
        "type": "string",
        "description": "第 1 个参数（lat1）"
      },
      "lon1": {
        "type": "string",
        "description": "第 2 个参数（lon1）"
      },
      "lat2": {
        "type": "string",
        "description": "第 3 个参数（lat2）"
      },
      "lon2": {
        "type": "string",
        "description": "第 4 个参数（lon2）"
      },
      "unit": {
        "type": "string",
        "description": "第 5 个参数（unit）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction haversine(lat1, lon1, lat2, lon2, unit){ unit=unit||'km'; var R=6371; if(unit==='mi') R=3958.8; if(unit==='m') R=6371000;\r\n  var toRad=function(d){return d*Math.PI/180;};\r\n  var dLat=toRad(lat2-lat1), dLon=toRad(lon2-lon1);\r\n  var a=Math.sin(dLat/2)*Math.sin(dLat/2)+Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)*Math.sin(dLon/2);\r\n  var c=2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a)); return R*c; }\r\nvar EXPORTS={haversine:haversine};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.HaversineForgePure = EXPORTS; }\r\n\r\n"
}