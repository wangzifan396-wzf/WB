{
  "id": "PolarForge",
  "name": "polar_forge",
  "title": "PolarForge",
  "description": "极坐标 ↔ 直角坐标互转：由 (x,y) 求 (r,θ°) 或反之，离线精确计算。",
  "entry": "toPolar",
  "entries": {
    "toPolar": {
      "paramStyle": "positional",
      "paramNames": [
        "x",
        "y"
      ],
      "fields": []
    },
    "toCartesian": {
      "paramStyle": "positional",
      "paramNames": [
        "r",
        "deg"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "x": {
        "type": "string",
        "description": "第 1 个参数（x）"
      },
      "y": {
        "type": "string",
        "description": "第 2 个参数（y）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction toPolar(x, y){ if(x===0&&y===0) return {r:0, deg:0}; var r=Math.sqrt(x*x+y*y); var a=Math.atan2(y,x)*180/Math.PI; if(a<0) a+=360; return {r:r, deg:a}; }\r\nfunction toCartesian(r, deg){ var a=deg*Math.PI/180; return {x:r*Math.cos(a), y:r*Math.sin(a)}; }\r\nvar EXPORTS = { toPolar:toPolar, toCartesian:toCartesian };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.PolarForgePure = EXPORTS; }\r\n\r\n"
}