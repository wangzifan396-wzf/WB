{
  "name": "QsvForge - nano-tools",
  "short_name": "QsvForge",
  "description": "像 qsv 一样用 SQL 查询 CSV：SELECT 列、WHERE（= != > < >= <= LIKE CONTAINS，支持 AND）、ORDER BY、LIMIT，全部在浏览器内完成。 · 单文件 · 零依赖 · 本地优先",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "background_color": "#0A0A0B",
  "theme_color": "#5E6AD2",
  "icons": [
    {
      "src": "og.svg",
      "sizes": "any",
      "type": "image/svg+xml",
      "purpose": "any"
    }
  ]
}
