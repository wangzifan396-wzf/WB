{
  "id": "SciNotationForge",
  "name": "sci_notation_forge",
  "title": "SciNotationForge",
  "description": "科学计数法：十进制 ↔ a×10ⁿ，含格式化与还原，离线可用。",
  "entry": "toSci",
  "entries": {
    "toSci": {
      "paramStyle": "single",
      "paramNames": [
        "x"
      ],
      "fields": []
    },
    "fromSci": {
      "paramStyle": "positional",
      "paramNames": [
        "m",
        "e"
      ],
      "fields": []
    },
    "formatSci": {
      "paramStyle": "single",
      "paramNames": [
        "x"
      ],
      "fields": []
    },
    "add": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    },
    "mul": {
      "paramStyle": "positional",
      "paramNames": [
        "a",
        "b"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "x": {
        "type": "string",
        "description": "输入（x）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nfunction toSci(x){ if(!isFinite(x)) return null; if(x===0) return {mantissa:0, exponent:0}; var neg=x<0; x=Math.abs(x); var e=Math.floor(Math.log10(x)); var m=x/Math.pow(10,e); if(neg) m=-m; return {mantissa:m, exponent:e}; }\r\nfunction fromSci(m, e){ return m*Math.pow(10, e); }\r\nfunction formatSci(x){ var s=toSci(x); if(!s) return String(x); var m=s.mantissa; var sign = s.exponent<0? \"e\" : \"e+\"; return m.toPrecision(6).replace(/\\.?0+$/,'') + sign + s.exponent; }\r\nfunction add(a, b){ return a+b; }\r\nfunction mul(a, b){ return a*b; }\r\nvar EXPORTS = { toSci:toSci, fromSci:fromSci, formatSci:formatSci, add:add, mul:mul };\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.SciNotationForgePure = EXPORTS; }\r\n\r\n"
}