{
  "id": "SafeZoneForge",
  "name": "safe_zone_forge",
  "title": "SafeZoneForge",
  "description": "按平台预设绘制视频安全区叠加",
  "entry": "safeRects",
  "entries": {
    "safeRects": {
      "paramStyle": "positional",
      "paramNames": [
        "w",
        "h",
        "preset"
      ],
      "fields": []
    }
  },
  "domFree": true,
  "schemaSource": "auto",
  "inputSchema": {
    "type": "object",
    "properties": {
      "w": {
        "type": "string",
        "description": "第 1 个参数（w）"
      },
      "h": {
        "type": "string",
        "description": "第 2 个参数（h）"
      },
      "preset": {
        "type": "string",
        "description": "第 3 个参数（preset）"
      }
    },
    "additionalProperties": false
  },
  "core": "\r\n\r\nvar PRESETS={ 'youtube':{action:0.90,title:0.80}, 'tiktok':{action:0.85,title:0.75}, 'instagram':{action:0.90,title:0.80}, 'broadcast':{action:0.90,title:0.80} };\r\nfunction safeRects(w,h,preset){ var p=PRESETS[preset]||PRESETS.youtube; var aw=Math.round(w*(1-p.action)/2), ah=Math.round(h*(1-p.action)/2);\r\n  var tw=Math.round(w*(1-p.title)/2), th=Math.round(h*(1-p.title)/2);\r\n  return {action:{x:aw,y:ah,w:w-2*aw,h:h-2*ah}, title:{x:tw,y:th,w:w-2*tw,h:h-2*th}}; }\r\nvar EXPORTS={safeRects:safeRects, PRESETS:PRESETS};\r\n\r\nif (typeof module !== 'undefined' && module.exports) { module.exports = EXPORTS; }\r\nif (typeof window !== 'undefined') { window.SafeZoneForgePure = EXPORTS; }\r\n\r\n"
}